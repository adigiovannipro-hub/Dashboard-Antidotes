# Antidotes

Plateforme de dashboards qui remplace la chaîne Supermetrics + Looker Studio, mais aussi tous les outils que j'utilise pour accompagner mes clients ou gérer mon entreprise : modération, finance, planning éditorial, faq... Un tout en un social media pro et perso. L'objectif est d'automatiser et optimiser un maximum mon travail quotidien tout en ayant accès à un dashboard de pilotage, ainsi que des accès clients pour ce qui les regarde.
Une seule app Next.js. Les données des régies sont synchronisées une fois par jour et stockées en base, puis lues localement : affichage instantané, historique illimité, coût d'infra nul.

Le cahier des charges d'origine est dans `PROMPT-V1.md` et fait foi en cas de doute produit, mais il ne couvre que la première feature, Reporting. Modération, Planning Éditorial et Reçus sont arrivés après, hors spec.
Premier client livré : Bondet.

## Comment travailler avec moi

Je ne suis pas développeur. Je décris des besoins produit, pas des solutions techniques.

- **Tranche seul sur le technique.** Choix de librairie, structure de fichiers, schéma de données, nommage : décide, applique, et explique en une ligne. Ne me demande pas d'arbitrer entre deux options techniques dont je ne peux pas évaluer les conséquences.
- **Challenge-moi.** Si je demande quelque chose qui va casser un truc, coûter cher, contredire l'architecture existante ou créer de la dette, dis-le avant de coder. Propose l'alternative. Tu as le droit de dire que ma demande est une mauvaise idée.
- **Demande-moi uniquement le produit et les secrets.** Ce que doit faire la fonctionnalité, à quoi ça ressemble, quelles clés API fournir.
- **Fonctionnel avant élégant.** Une feature qui marche de bout en bout bat une abstraction propre à moitié branchée. Pas de refacto non demandée.
- **Jamais de faux positif.** Ne dis pas que c'est fait sans preuve : sortie de test, build qui passe, ou URL de preview. Si un bout est encore en démo ou en mock, dis-le explicitement.
- Réponds en français.

## Architecture des espaces

Deux mondes, une frontière étanche entre les deux.

**Mon espace (`/`, `/finance`, `/entreprise/*`)** — tout ce qui est à moi, perso et pro confondus. La frontière perso/pro est volontairement mince : une page Finance de type Finary agrège l'ensemble (comptes, trésorerie EUR, investissements, facturation client à venir), sans cloisonner. Contient aussi la todo et les outils internes : Modération, Reçus.

**Espaces clients (`/espace/[workspace]/*`)** — un dossier par client, cloisonné : reporting, FAQ, planning éditorial, stratégie. Un client ne voit jamais un outil interne, jamais un autre client, jamais une donnée financière.

L'accès se règle **page par page**, depuis les trois points d'un client dans le rail : `workspace_page_grants` (0035) porte, par adresse, les pages masquées d'un espace — absence de ligne valant visible. Le réglage se prend au moment de l'invitation, donc avant que le compte existe, ce qui explique la clé par email et non par utilisateur. Il porte sur la **visibilité d'une page**, pas sur un niveau lecture/écriture : à l'intérieur d'une page ouverte, un client écrit déjà chez lui, et `tests/planning-isolation.test.ts` encode cette décision. Le Contexte n'y figure jamais.

Règle absolue : toute nouvelle table est protégée par RLS et accompagnée d'un test d'isolation avant d'être considérée comme finie. Le cloisonnement client n'est jamais assuré uniquement par le filtrage côté application.

Le modèle à copier est **`tests/planning-isolation.test.ts`**. Il ouvre de vraies sessions Supabase, attaque l'API REST directement — hors de toute interface — et vérifie les **deux sens** de la règle : un client ne lit ni ne modifie l'espace voisin, **et** il écrit bien chez lui. C'est la moitié qu'on oublie de tester : une politique trop stricte casse le produit aussi sûrement qu'une politique trop large le rend dangereux. `tests/isolation.test.ts` couvre le socle (organisations, espaces, dashboards, liens de partage).

Les huit modules ont leur suite : `tests/isolation.test.ts` (socle), `planning-isolation`, `receipts-isolation`, `finance-isolation`, `mon-travail-isolation`, `contexte-isolation` (le client ne lit **rien**, pas même son propre espace ; le contributeur lit et écrit le sien, jamais celui du voisin), `droits-pages-isolation` (le partenaire lit ses propres droits mais ne peut ni se rouvrir une page, ni en masquer une à un tiers), `production-isolation` (le cycle, les jobs, les accroches — et la **synthèse mensuelle**, que le client ne lit ni ne réécrit sur son propre compte), `social-isolation` (le client ne voit ni l'inventaire de l'agence, ni un jeton — pas même celui du compte qu'on lui a affecté — et ne s'affecte rien lui-même). Elles ont **toutes tourné vertes contre le vrai projet Supabase** le 14/08/2026 (run db-admin 31792209507, 10 fichiers, 120 tests, 90 s), migrations 0043 et 0044 appliquées au passage — elles sautent toujours en local tant que `.env.local` n'est pas renseigné, et la CI de push ne les exécute jamais : le vert d'un push ne prouve rien, celui de db-admin si. Depuis : `billing-isolation`, `web-isolation` et `academy-isolation` ont rejoint et tourné vertes ; `faq-moderation-isolation` (la FAQ ouverte aux espaces, 20260830) est écrite mais **n'a jamais tourné verte** — sa migration part avec le prochain push sur `main`, la rejouer ensuite.

Un module interne renvoie **404 et non 403** à qui n'y a pas droit : un client ne doit pas apprendre l'existence de la Modération en tombant sur un « accès refusé ».

## État actuel

| Chantier | Où | État |
|---|---|---|
| Fondations | `supabase/migrations/0001-0003`, `src/lib/auth.ts`, `src/app/admin/acces` | 16 tables, RLS + tests d'isolation réels, auth magic link, hub, navigation, invitations |
| Système visuel | `src/styles/tokens.css`, `src/app/globals.css`, `src/components/ds/` | Canvas gris chaud / surfaces blanches, tokens uniques, échelle typographique `type-*`, Inter + Montserrat, rail latéral repliable, primitives Panel / NavCard / StatCard / StatusPill. Contraste 4,5:1 vérifié au navigateur sur les huit pages |
| Reporting | `/espace/[workspace]/[dashboard]`, `src/components/viz/`, `src/lib/reporting/` | **Trois bandes** : les chiffres (ROAS couché + tuiles + entonnoir vertical à droite), l'audience (Persona en trois listes de barres à gauche, courbe d'abonnés à droite, hauteur commune), le détail par ad set. Tuile = pastille et libellé à gauche, chiffre et variation **contre le bord droit** pour que les nombres s'alignent en colonne. **Onglets par réseau** (`networks.ts`, testé) : un par compte affecté dans Connexions, ordre figé Meta Ads / Instagram / Facebook — un onglet doit tomber au même endroit d'un client à l'autre ; le Contexte sert à dire ce qui **manque**, et l'état vide renvoie à Connexions. **Sélecteur de période à plage** (`period.ts`, testé) : préréglages (mois dernier, mois en cours, 7/30/90 jours) + deux dates libres, Annuler/Appliquer — rien ne part à la frappe. Bornes hautes à **hier** : inclure aujourd'hui ferait baisser chaque chiffre selon l'heure de consultation. Plage en ISO dans l'URL (`?du=&au=`), prime sur `?mois=`. L'entonnoir est à l'**encre** verte, pas à la teinte de série : blanc sur le vert de marque = 2,71:1, refusé par l'audit. Le jeu de démonstration ne couvrant que juin 2026, c'est ce mois qui s'ouvre, et tout autre mois affiche un état vide qui le dit plutôt que les chiffres de juin sous une autre étiquette. `RawMetrics` a gagné `reach`, `addToCart` et `initiatedCheckout` — 47 et 34 viennent du Looker réel, la portée est **provisoire** en attendant le premier passage réel. **Le connecteur Meta est construit** (`src/lib/connectors/meta/` — `mapping.ts` et `organic.ts` purs et testés, `graph.ts` transport paginé, `sync.ts` orchestration) : Insights ad set au grain jour, ventilations âge/genre/région agrégées d'un seul appel, médias Instagram avec insights en expansion de champ et repli sans elles, posts de Page, abonnés en instantané quotidien — upserts par identifiant externe, un `sync_run` par passage, chaque `error` Supabase testé. Fenêtre 90 j au premier passage puis 35 j (Meta réécrit les conversions sur 28 j) ; la portée est stockée **au grain jour** (approximation additive, celle de Supermetrics — migration 0045). Déclencheurs : bouton « Synchroniser » du propriétaire (`POST /api/reporting/sync`, 404 pour un client) et cron Vercel quotidien `/api/cron/sync-reporting` à 5h UTC (Meta accepte les IP Vercel). **Les données réelles priment dès qu'elles existent pour la période ; la démo Bondet ne survit qu'en repli sur juin 2026** — dès que juillet réel arrive, il s'ouvre par défaut. **Dashboard organique** (`organic-dashboard.tsx`) : **taux d'engagement en héros** (interactions / portée, repli sur les vues quand Meta ne rend pas la portée — c'est pourquoi la portée a quitté les tuiles Instagram), tuiles du contrat `kpi-sets`, courbe d'abonnés, performance par publication. **Les deux tableaux se trient au clic et portent une ligne de total** : sommes pour les grandeurs additives, **ratio recalculé sur les agrégats** pour un taux (`posts-table.tsx`) — la moyenne des taux ligne à ligne donnerait à un post confidentiel le poids d'un reel vu cent mille fois. La **carte des mesures** met le texte à gauche et la pastille à droite ; l'**entonnoir** est une bande horizontale — une marche par bloc, taux de passage en pastille entre deux marches — après trois formes refusées (trapèzes pleins, barres empilées, colonne étroite à droite des tuiles) ; la **heatmap est divergente** (meilleurs en vert, pires en rouge). **Un seul relevé d'abonnés n'affiche pas une courbe** (`followers-card.tsx`) : deux graduations identiques et aucune ligne se lisent comme une panne — on montre le chiffre et la date du prochain point. **`errors.ts` traduit les refus de Meta** en français avec le geste à faire — `(#10) pages_read_user_content` = jeton antérieur à l'ajout de la portée, rebrancher suffit (**accès standard : la permission fonctionne pour les comptes à rôle dans l'app**, l'App Review ne sert qu'au grand public — mais elle doit être **ajoutée à l'app dans la console Meta**, sinon le dialogue refuse tout en bloc sur « Invalid Scopes », vécu une fois) ; jeton expiré ; plafond d'appels, le seul où rebrancher ne sert à rien. **Un refus sur les publications d'une Page ne fait plus tomber la source** : abonnés et vitrine passent, le refus devient un avertissement affiché en tête de page. Vérifié au navigateur sur des lignes posées en base (bac PostgREST), audit de contraste sans violation. **Jamais lancé contre le vrai Meta** : rebrancher le compte (portées insights absentes du jeton), appliquer 0045, puis Synchroniser |
| Reporting Site Web | onglet `site-web` du Reporting, `src/lib/connectors/google-analytics/`, `src/lib/web/`, migrations 0060-0061 | **Première source Composio réelle** : le trafic GA4 d'un client (ANMF, propriété `properties/428494328`), répliqué du rapport Looker — 4 cartes à double courbe (période vs **année N-1**, jamais le mois d'avant : un site est saisonnier), Persona (nouveaux/connus, appareils, villes), sources empilées par mois sur l'année écoulée, Top Pages triable avec total recalculé sur les agrégats. **Deux grains en base** : le jour pour les courbes et les sommes additives, le mois civil pour les visiteurs uniques que GA dédoublonne par période — sommer des uniques quotidiens surcompte, et l'écran retombe sur cette somme (en le disant) seulement sur une plage libre. Durée de session stockée **cumulée** (moyenne × sessions), rebond recalculé (1 − engagées/sessions). Collecte via `GOOGLE_ANALYTICS_RUN_REPORT` (Composio, `COMPOSIO_API_KEY`), **mois par mois** — une grosse réponse est rangée en fichier par Composio au lieu d'être rendue, et le transport refuse ce cas plutôt que de le lire comme « zéro ligne » (payé une fois au rattrapage ANMF) — fenêtre 8 jours glissants, histoire entière au premier passage, villes bornées au top 50 mensuel, greffée au cron `/api/cron/sync-reporting` et au bouton Synchroniser. Rattachement par `pnpm connect:web`, rattrapage par `pnpm sync:web`. `tests/web-isolation.test.ts` couvre les deux sens plus l'interdiction d'écrire (tables en lecture seule pour `authenticated`). Démarches : `docs/web-analytics-setup.md` |
| Reporting LinkedIn | onglet `linkedin` du Reporting, `src/lib/connectors/linkedin/`, migrations 20260902c-f | **Deuxième source Composio, et la première par passage HTTP brut.** LinkedIn ne passe pas par un OAuth d'ici : l'autorisation vit chez Composio, un seul branchement couvre les six pages de l'agence (`docs/linkedin-connexion.md`). **Les outils pré-emballés de la passerelle ne suffisaient pas** — `GET_SHARE_STATS` refuse tout `timeIntervals`, aucun outil n'expose les publications — et c'était une limite de l'emballage, pas de LinkedIn : `tools.proxyExecute` rend la main sur l'URL, les en-têtes et la syntaxe RestLi, en gardant l'authentification chez Composio (aucun jeton ne transite). Quatre lectures, toutes **sondées sur pièce** : statistiques de la page au grain **jour** (`/v2/…ShareStatistics` + intervalle), **liste des publications** (`/rest/posts`, 627 sur ANMF), **statistiques par publication** (`ugcPosts=List(…)`), **gains d'abonnés mensuels**. LinkedIn se range donc dans les **mêmes tables** qu'Instagram — `social_posts`, `social_page_daily`, `social_followers` — et se lit par `getOrganicData`, sans code à lui. Grandeurs : impressions, portée, clics, réactions, commentaires, partages ; ni enregistrements ni vues vidéo, LinkedIn n'en a pas. **La courbe d'abonnés se reconstruit** : le compte d'aujourd'hui moins les gains des mois suivants donne treize mois d'historique au premier passage, là où Meta repart de zéro. Les tuiles viennent de **la page** et non de la somme des publications — ses impressions couvrent aussi les posts parus avant la période et encore vus pendant (`withPageFallback` étendu aux six grandeurs). `mapping.ts` et `errors.ts` purs et testés sur les charges utiles réelles. **Les routes `/rest/` périment** : LinkedIn ne garde qu'un an de versions et la fenêtre n'est pas continue (202606 vivante, 202512 morte, 202510 vivante) — `LINKEDIN_API_VERSION` la surcharge, et le connecteur préfère `/v2`, sans version, partout où elle suffit. **Collecté en production sur ANMF** (2/09) : douze mois pleins, d'août 2026 (36 903 impressions, 22 666 de portée, 1 671 clics, 817 interactions, 13 publications) à septembre 2025, plus treize points d'abonnés de 5 636 à 6 711. Deux pièges payés au premier passage réel — LinkedIn mêle `urn:li:ugcPost:` et `urn:li:share:` dans le même fil et **chacun a son paramètre** (un `share` dans `ugcPosts` fait échouer le lot entier en 400, toutes les publications perdant leurs statistiques), et la reconstruction datait un relevé du dernier jour du **mois en cours**, donc du futur : un upsert ne le retire pas, le connecteur efface ce qui dépasse le jour clôturé. Seule exception au passage brut : les abonnés, où `/v2/networkSizes` refuse `edgeType` à travers le proxy et où l'outil emballé fait mieux. **Les chiffres ont été vérifiés contre LinkedIn lui-même** (2/09) : les statistiques natives de la page ANMF affichent 35 519 impressions et 787 réactions du 2 au 31 août, nous 36 903 et 809 sur le mois entier — l'écart est exactement le 1er août. Le Looker Studio du client, lui, en affiche 3 722 : il ne correspond à aucune des six pages administrées, ni au même mois de 2025 (36 847), ni à la somme des publications parues dans le mois (26 094) — **c'est le rapport qui est faux**, et c'est le sonde `pnpm sonde:linkedin --organisation --mois` qui l'a établi, pas une relecture. Corollaire, **le taux d'engagement de l'onglet LinkedIn suit la définition de LinkedIn** et non celle d'Instagram : clics compris, rapportés aux **impressions** (`engagementRateWithClicks`) — 6,73 % calculé ici contre 6,8 % affiché par le réseau, quand la formule d'Instagram aurait donné 3,6 %. Les **vues de la page** s'ajoutent (0902g, `page_views`/`unique_page_views`/`jobs_page_views` sur `social_page_daily`, `/rest/organizationPageStatistics` au grain jour par tranches de 90 j) : ce n'est pas une vue de contenu, personne n'y tombe en faisant défiler son fil — et l'onglet Emplois est ce qu'un client qui recrute regarde en premier. `unique_page_views` est stockée mais **jamais sommée** sur une période, même règle que les visiteurs uniques du web. Les **vignettes** viennent de `/rest/images` et `/rest/videos` (`downloadUrl` pour une image, `thumbnail` pour une vidéo — surtout pas son mp4 ; un document n'a qu'un PDF, donc rien) ; ces URL périment en une semaine et se réécrivent à chaque passage, ce que la fenêtre glissante fait naturellement. Le texte des publications est **déséchappé** (`decodeCommentary`) : LinkedIn rend du « Little Text », et `{hashtag|\#|Métiers} \| 🔬` s'affichait tel quel. **Rattrapé en production le 2/09** (run db-admin 33649474902, 521 lignes, treize mois) : août rend 36 903 impressions, 22 666 de portée, 1 671 clics, 817 interactions, **316 vues de page dont 42 sur les offres d'emploi**, et 12 publications sur 13 ont récupéré leur vignette — la treizième est un document, dont LinkedIn ne rend qu'un PDF. Rattrapage par `pnpm sync:linkedin` (`--espace`, `--depuis`), qui **relit ce qu'il vient d'écrire** et affiche le récapitulatif mensuel — « ✓ 54 lignes » ne dit pas si août est là ; greffé au cron et au bouton Synchroniser |
| Modération (interne) | `/moderation`, `src/lib/moderation/` | 13 tables, pgvector, FAQ sémantique, génération Claude, boucle d'apprentissage. **Inbox croisée tous clients** : `/moderation` est le seul écran — l'ancienne route par client redirige (`?client=`), la FAQ reste par client. Onglets de canaux façon Boîte de réception Meta (Tout / Commentaires Instagram / Commentaires Facebook / Messages privés) avec compteurs d'à-traiter, jetons clients au logo de leur espace, statuts groupés (à traiter / en attente / traitées / toutes) + non-lus + signalées — le tout dans l'URL, et **les compteurs se répondent** : les onglets comptent dans le client choisi, les clients dans l'onglet choisi, jamais un badge qui promet ce que le clic ne montre pas. Plus de bande de mesures ni de rail de filtres ; à partir de `lg` le panneau est borné à l'écran (chaque volet défile chez lui, les actions du fil restent sous la main) ; sur mobile, liste puis fil plein écran avec retour. **La synchronisation Meta est réelle** (`src/lib/moderation/sync.ts`) : les commentaires Instagram et Facebook des comptes affectés dans Connexions remontent chaque heure (`pnpm sync:moderation`, étape du workflow horaire) et au bouton « Relever maintenant » du propriétaire (`POST /api/moderation/sync`, 404 sinon — et le module s'affiche à l'owner même vide, pour le premier relevé). Un fil = un commentaire de tête et ses réponses ; la conversation porte la **publication commentée** (0049 : permalien, extrait, vignette — affichée en liste et en tête de fil) ; une réponse de la marque faite ailleurs classe le fil `answered_elsewhere`, un entrant nouveau **rouvre** même un fil ignoré, le triage lexical pose signalements et langue à l'ingestion — la fusion est pure et testée (`ingest.ts`, `connectors/meta/comments.ts`). `channel_connections` sert de journal de passage (dernier relevé et erreurs affichés en tête d'inbox), et le client de modération d'un espace est trouvé, **adopté par slug** (démo) ou créé au premier relevé. **Valider ou corriger publie réellement la réponse sous le commentaire** (`send.ts`, jeton lu de `social_account_secrets` après la garde opérateur ; preuve en base — message sortant origine `antidotes`, identifiant Graph ; refus Meta traduits en français, échec = `send_failed` visible ; canal non branché = validé avec la raison, jamais un faux « envoyé »). Portées `instagram_manage_comments` et `pages_manage_engagement` ajoutées : **les déclarer dans la console Meta avant de rebrancher**, sinon « Invalid Scopes » refuse toute la connexion. **L'indexation sémantique a quitté le clic** (vécu au premier envoi réel : le modèle d'embeddings ne charge pas sur Vercel — voir Pièges connus — et la correction entière échouait, réponse comprise) : une correction écrit son entrée FAQ **sans vecteur** (`embedding_source` null) et `reindexFaqSearch`, étape du relevé horaire comme du bouton, indexe ce qui manque et fait converger les vecteurs `deterministic` de la démo vers le modèle réel — deux sources ne se comparent pas ; une entrée sans vecteur est simplement invisible de la recherche sémantique jusqu'au passage suivant, jamais un blocage de l'envoi. **Les messages privés sont branchés** (`connectors/meta/messages.ts`, pur et testé) : Messenger et DM Instagram passent tous deux par `/{page}/conversations`, la boîte d'un compte Instagram professionnel appartenant à sa Page — d'où `parent_external_id`, sans quoi il n'y a rien à relever. Répondre écrit à **la personne** (`/{page}/messages`, `recipient.id`), pas à la conversation, et pose le tag `human_agent` seulement au-delà de 24 h : le poser toujours déclarerait une intervention humaine là où une réponse immédiate suffit. **Un refus sur la messagerie ne fait pas tomber les commentaires** — portées distinctes, l'avertissement s'affiche et le reste passe. Portées `pages_messaging` et `instagram_manage_messages` en plus des deux précédentes, mêmes conditions de console. **Les gestes de rangement sont ceux de Meta, sans colonne nouvelle** (`applyInboxGesture`, une seule action pour l'unité et le lot) : lu/non-lu = `unread`, archiver = `ignored` — relabellisé « Archivé », c'est un rangement et non un refus —, supprimer = `deleted_at`, que le relevé suivant respecte. Rien n'est effacé chez Meta : le commentaire reste en ligne, c'est notre boîte qu'on range. Chaque ligne porte une **coche à gauche** et, au survol, une **vignette de deux carrés en haut à droite** — lu au-dessus, archiver dessous ; la sélection allume une **barre flottante** hors du panneau à gauche (en bas et horizontale sous `lg`, faute de place). La vignette a besoin d'un `z-10` explicite : le bouton d'ouverture de la ligne, positionné et déclaré après, avalait ses clics — elle se voyait et ne réagissait pas. **Une zone de saisie libre en bas du fil** répond hors FAQ (`sendManualReply`, même chemin d'envoi que la validation, ⌘+Entrée), et un bouton **Répondre** sous chaque message entrant préfixe la mention `@pseudo`. **Les pièces jointes remontent** : sur Facebook une réponse en GIF est un commentaire au texte vide, invisible sans le champ `attachment` — le fil l'affiche, la liste écrit « GIF », et une URL Meta périmée retombe sur le libellé cliquable plutôt que sur une image cassée. **Un auteur que Meta ne nomme pas** (compte personnel restreint) est redemandé un par un (`fetchCommentAuthors`), puis dit « Auteur masqué par Meta » — « Inconnu » se lisait comme une panne. La navigation entre fils ne fait plus attendre : le fil demandé part **en même temps** que la liste, et le surlignage est optimiste — 94 ms mesurées au lieu d'un aller-retour serveur complet. `pnpm purge:moderation-demo` efface la démonstration le jour où le réel prend le relais — et **sait distinguer les deux cas**, parce que le relevé *adopte* un client de démo qui porte le slug d'un espace : client jamais adopté, la ligne part avec son contenu ; client devenu réel, seules les lignes du seed partent (`thread_%`, `demo_%`, `story-%`), la FAQ et les vraies conversations restent. Supprimer la ligne aurait emporté par cascade tout le réel de Bondet. **YouTube est le deuxième connecteur** (`src/lib/connectors/youtube/`, mapping et traduction des refus purs et testés) : les commentaires d'une chaîne remontent par `commentThreads.list?allThreadsRelatedToChannelId` — un appel pour toute la chaîne, pas un par vidéo — la vidéo commentée sert de publication, et répondre passe par `comments.insert` au niveau du commentaire de tête, comme sur Instagram. **Le jeton Google n'est pas un jeton Meta** : une heure de vie, un jeton de rafraîchissement permanent à côté, donc un JSON chiffré là où Meta n'a qu'une chaîne (`credentials.ts`), renouvelé et réécrit au relevé. Branchement par son propre aller-retour Google (`/api/social/youtube/connexion`), **chaîne par chaîne** : là où un login Meta couvre toute l'agence, YouTube demande l'accord du compte qui administre la chaîne du client. Le bouton n'apparaît que si YouTube est déclaré aux livrables du Contexte. Quota de 10 000 unités/jour : une page de 100 fils coûte 1, une réponse 50 — c'est la réponse qui pèse, et elle est humaine ; un quota épuisé se dit et ne propose pas de rebrancher. Démarches dans `docs/youtube-connexion.md`, dont le piège du mode « Testing » de Google, qui périme le jeton en sept jours. **Le relevé Meta tourne en production** (l'inbox I-WAY réelle remonte) ; le premier envoi réel d'une correction a échoué sur le chargement du modèle d'embeddings — corrigé, l'envoi ne dépend plus de l'indexation — et **reste à re-tester depuis l'écran**. YouTube n'a jamais tourné contre le vrai service. Les branchements directs sont **rouverts** (`src/lib/social/direct-connect.ts`) — la bascule Composio les avait fermés, et le compte Meta a été rebranché avec les quatorze portées le 30/08. **L'état de lecture se miroir dans les deux sens** : `unread_count` des conversations Meta éteint le non-lu à l'ingestion (`platformUnread`, lire n'est pas répondre — le statut ne bouge pas), et le geste « lu »/« archiver » comme l'ouverture d'un fil postent `sender_action=mark_seen` chez Meta (`markSeenOnPlatform`, best-effort dans `after()`). **Les avatars ont trois chemins** : la photo du participant relevée à l'ingestion, un rattrapage par lots pour les DM (`fetchMessagingProfiles`, plafonné à 50 par relevé), et des initiales sur teinte stable sinon — `ParticipantAvatar` porte le badge du canal en haut à droite et la bulle du logo client en bas à droite. **Le texte entrant est assaini avant l'upsert** (`sanitizeText`) : PostgREST caste la charge en json, et un caractère de contrôle brut ou un demi-surrogate dans un seul commentaire faisait échouer le lot entier (« invalid input syntax for type json ») — c'était la cause réelle de canaux entiers en erreur. **Une liste de médias Instagram trop lourde se réessaie en fenêtres décroissantes** (21 puis 7 jours) sur le refus de volume de Meta, et sur lui seul. **La FAQ vit dans le Planning, pas dans une page à elle** (`src/components/planning/faq-moderation-board.tsx`) : l'onglet FAQ de la section Planning — là où était le board Monday — rend les `faq_entries` de la Modération en tableau façon Monday : Élément / Thème en étiquette colorée stable (empreinte du nom, « Général » gris) / Question / Réponse / Réponse TikTok tronquées à la ligne / bulle Client / Mise à jour, tri par colonne, ⌘F, panneau latéral d'édition au clic (`?entree=` l'ouvre — l'ancien lien du fil redirige). Une page dédiée `/espace/[workspace]/faq-moderation` a été **construite puis refusée et supprimée** — la FAQ appartient au Planning ; `/moderation/[client]/faq` n'est plus qu'une redirection. Le circuit de validation client reste : « Demander la validation » pose `client_review = pending`, le client tranche Valider/Refuser depuis le même tableau, le verdict s'écrit par action serveur à garde applicative, jamais en écriture directe (`tests/faq-moderation-isolation.test.ts`, **10/10 verts contre la vraie base** le 30/08, migration 20260830 appliquée par le job migrer). Les 68 entrées des trois boards Monday « FAQ MODÉRATION » sont **importées en production** (`pnpm import:faq-moderation`, figées dans `scripts/data/faq-moderation-monday.json`, idempotent par client + titre). L'ancienne table `planning_faq_entries` ne sert plus qu'aux espaces sans client de modération |
| Planning Éditorial | `/espace/[workspace]/planning`, `src/lib/planning/` | 8 tables — le board Monday **natif et éditable** : mois → réseaux → publications, **ordre manuel par drag & drop** (poignée à gauche, dépôt entre lignes, dans un autre réseau ou un mois replié qui s'ouvre au survol), colonnes personnalisables et redimensionnables (0009), étiquettes libres éditables depuis le sélecteur d'une cellule (0029), journal d'activité **en phrases** (« a ajouté un visuel »), recherche ⌘F, **état de lecture mémorisé par tableau** (tri, mois ouverts, réseaux repliés — cookie `antidotes_planning_<espace>_<tableau>`, relu côté serveur ; `months: null` = jamais touché ≠ liste vide = tout replié), panneau latéral **local d'abord** (ouverture/fermeture sans aller-retour serveur, glissade de sortie, Échap ferme — visionneuse d'abord si ouverte), carrousel à **cartes arrondies bordées avec aperçu de la suivante** (ni flou ni aplat), visionneuse plein écran même langage, cellule Visuel = panneau + glisser-déposer de fichiers + spinner d'envoi, retours avec **tag `@` en popover** envoyés via la boîte Gmail des Reçus (0030), sélection multiple persistante : Dupliquer / **Déplacer (« Choisir un nouveau parent »)** / **Archiver** / Supprimer ; **archives et corbeille restaurables** dans l'en-tête (0031, suppression douce des publications et des mois). **Prévisualisation du feed Instagram** par mois (`src/lib/planning/feed.ts`, testé) : grille 3 colonnes antichronologique à fin de mois, mois précédents compris — un feed ne commence pas au 1er ; première image d'un carrousel en couverture, vidéo à sa première image (`#t=0.1`, aucune extraction serveur), Stories exclues et Reels non, bascule 4:5 / 1:1, en-tête repris du compte branché. **Un sujet sans créa garde sa case**, en gris pointillé daté sur fond blanc : c'est le trou qu'on vient chercher des yeux, le masquer rendrait la grille menteuse. **Les deux panneaux latéraux cohabitent** (`panel-layers.ts`) : une case du feed ouvre sa publication par-dessus, le dernier panneau demandé passe devant, et un clic sur le tableau referme les deux — le rappel de fermeture passe par une référence, sans quoi le premier panneau à réagir désinscrit l'écouteur du second au milieu de la propagation et un seul se ferme. **Branchement des comptes sociaux** en boîte depuis l'en-tête du tableau : un aller-retour OAuth Meta rapporte Pages, comptes Instagram rattachés et comptes publicitaires, chemin de retour porté par le cookie d'état. Sans `META_APP_ID`, la boîte dit quoi renseigner plutôt que d'offrir un bouton mort. **Le modèle a deux étages** (0043 puis 0044, appliquées) : `social_accounts` est l'**inventaire de l'agence** — un seul login Meta atteint les comptes de tous les clients, cinq comptes Instagram au premier essai — et `workspace_social_accounts` porte l'**affectation**, un compte par réseau et par espace, clé primaire `(workspace_id, kind)` pour que « où publie-t-on ? » n'ait jamais deux réponses. 0043 rangeait tout par espace : les cinq comptes atterrissaient chez Bondet, rien ne disait sur lequel publier, et un partenaire y lisait le nom des comptes des autres clients. **Les jetons ont leur table** (`social_account_secrets`, owner-only) : sans ce découpage, ouvrir la vitrine du profil à un membre de l'espace — ce dont l'en-tête du feed a besoin — lui ouvrirait la ligne entière, la RLS filtrant des lignes et non des colonnes. Aucune affectation n'est devinée au passage : « Lunettes BONDET » a beau ressembler à l'espace Bondet, les listes s'ouvrent vides. **L'écran des connexions suit le contrat, plus une liste en dur** (0049) : il affichait trois lignes — celles que Meta rapporte — quel que soit le client, si bien qu'un client sur LinkedIn et YouTube n'en voyait aucune trace. Les lignes viennent maintenant des **réseaux déclarés aux livrables du Contexte** (`src/lib/social/networks.ts`, pur et testé), plus le compte publicitaire — que personne ne déclare, et qui alimente le Reporting — plus **tout compte déjà affecté** : retirer un réseau du contrat ne doit pas faire disparaître de l'écran une connexion qui continue de publier. `social_account_kind` gagne YouTube, Pinterest, X, Threads et Snapchat ; seul Meta a un connecteur, et `CONNECTABLE_KINDS` le dit à l'écran plutôt que d'offrir une liste vide qui se lirait comme une panne. Un réseau s'ajoute depuis cet écran et rejoint **les livrables**, pas une liste parallèle : la déclaration d'un client garde une seule source. Le bouton Connexions ne s'affiche qu'au propriétaire, `tests/social-isolation.test.ts` encode les deux sens. Import Monday et analyses toujours là. **Publication automatique** (`src/lib/publishing/`, migration 0046) : chaque heure le workflow GitHub exécute `pnpm publier:planning`, qui ne travaille qu'à **16h heure de Paris** — tout sujet **validé** (`status = 'validated'`) daté du jour part sur les réseaux de son couloir (META = Instagram + Facebook). Instagram en deux temps (conteneur, attente `FINISHED`, publication — vidéo seule = reel, plusieurs visuels = carrousel dans l'ordre de la ligne), Page avec son jeton de Page. **Stories exclues** (widgets impossibles par l'API — publication manuelle), un sujet **en retard ne part pas**. `planning_publications` est à la fois le verrou anti-double (unicité sujet × réseau), la preuve (identifiant, permalien) et l'erreur visible ; chaque geste s'écrit au journal du sujet (« Antidotes a publié automatiquement »). Bloqueurs vérifiés avant l'appel : visuel, wording (placeholders compris), reel sans vidéo, carrousel > 10. **« + 2027 » au bout des onglets** : prolonge le tableau d'une année — colonnes, étiquettes et réglages repris, douze mois vides, jamais le contenu. Le fichier de rattrapage idempotent (0009→0031, transmis dans la conversation) met la vraie base à niveau d'un seul collage — la liaison Supabase↔GitHub n'applique pas nos migrations, leur nommage `NNNN_` n'est pas celui de la CLI Supabase . **Le board s'est rapproché de Monday** — trois reproches, trois causes. *Les filets* : les cellules portaient `border/50`, invisible sur blanc ; elles sont à `border-border-strong`, en-tête de colonnes comprise, et chaque cellule respire d'un demi-cran (`py-1.5`). *L'information qui se perd* : douze mois espacés de 20 px se lisaient comme douze cartes du même poids, dont onze vides. Serrés (`space-y-1.5`), ils forment une pile ; le **rail gauche colore l'année** — encre pour le mois ouvert, vert de marque pour un mois qui porte du travail, effacé pour un mois vide — et le titre d'un mois vide passe à l'encre secondaire. Les sept « Mois vide » ont disparu : le rail et l'encre le disent déjà, la phrase ne servait qu'à répéter. Le compteur d'un mois est doublé des **pastilles des réseaux qu'il contient** : replié, il dit combien et sur quoi, là où « 3 · publications · 1 réseau » demandait de lire pour apprendre moins. L'en-tête d'un couloir passe au **blanc** — deux bandes grises collées (nom du réseau, puis noms de colonnes) formaient un bloc de 60 px où le réseau se noyait. Les **en-têtes s'alignent sur leur contenu** (texte à gauche, nombre à droite, étiquette centrée) : « SUJET » centré au-dessus de titres calés à gauche est un décalage que l'œil paie à chaque ligne. Le **sujet est l'ancre de la ligne** et reprend la place que le wording lui prenait (`minmax(230px,2fr)` contre `minmax(170px,1.2fr)`) — « Coulisses de l'atelier luneti » coupé en plein mot ne dit plus rien ; la largeur totale du tableau, elle, ne bouge pas. Un montant se lit `1 200` au repos et redevient brut dès qu'on entre dans la cellule (`formatPlainNumber`), et « Sponsorisation » devient « Sponso », qui tient dans sa colonne. *Les logos* : le huit couché de Meta, tracé à 14 px, se lisait « co » — **META n'a plus de pastille propre mais le duo Instagram + Facebook**, qui est littéralement ce que le couloir publie ; son filet garde le bleu Meta, prendre celui du premier membre peignait un rail rose sur toute la hauteur. Audit de contraste rejoué au navigateur, **étendu aux `<input>`** — les cellules du planning en sont, et leur valeur échappait entièrement au parcours des nœuds texte : une seule violation trouvée, le compteur d'un couloir à 3,9:1, passé à l'encre primaire. Vérifié sur `build && start`, clair et sombre |
| Reçus | panneau de `/entreprise/finance`, `src/lib/recus/` | 5 tables, Gmail → facture → Airwallex, vérification d'accrochage, auto-transfert à 3 validations concordantes, sync horaire depuis GitHub. La page dédiée a disparu au profit d'un panneau à une ligne par pièce : envoyer, archiver, toujours automatiser. L'historique des pièces parties est derrière l'icône d'archives de l'en-tête. Archiver écrit `ignored` ; c'est `forwarded_at` qui distingue le rangement du refus, et le statut `archived` ajouté par 0025 n'est écrit nulle part — la migration reste inutile à appliquer |
| Finance (phase 2.1) | `/entreprise/finance`, `src/lib/finance/` | 8 tables, bande de mesures (disponible masquable à l'œil, à facturer, en retard, dépensé), facturation à venir (lue du module Échéances), courbe solde vert + dépenses rouges 7/30/90 j, dépenses carte avec pastilles d'initiales et filtres de date JJ/MM/AAAA. **Sur données 100 % réelles depuis le 7 août** : `airwallex-sync.yml` horaire depuis GitHub (Airwallex bloque les IP Vercel), démo purgée, 46 soldes, ~100 dépenses, 30 factures. Les factures pilotent l'écran : statuts lus du couple `FINALIZED` + `payment_status`, noms de clients résolus via `/api/v1/billing_customers/{id}` (seul chemin qui réponde — les variantes rendent 401, pas 404). Logos de marchands récupérés au fil du sync (bucket privé `merchant-logos`, URL signées). Purge rejouable : Actions → Base de données. **Le rafraîchissement ne dépend plus du seul cron** : ouvrir Finance ou Échéances lance la collecte quand la dernière remonte à plus de dix minutes — voir la ligne « Synchronisation à la demande » plus bas. **La catégorie d'une dépense s'édite dans la ligne** (sélecteur natif, owner seulement) et **le geste s'apprend** : recatégoriser écrit une règle par marchand (`finance_category_rules`, upsert `org_id,matcher`) — le prélèvement du mois prochain arrive déjà rangé, et retirer la catégorie efface la règle. **Une catégorie personnalisée se crée depuis le sélecteur même** (« + Nouvelle catégorie… », dialogue à un champ) : créée, appliquée à la ligne et mémorisée pour le marchand d'un seul geste — l'identité est le slug normalisé (`slugifyCategoryName`, testé), un nom déjà pris sous une autre écriture range dans l'existante plutôt que de scinder le camembert en deux parts du même sens ; aucune migration, `finance_categories` et sa RLS owner-write existaient depuis 0012. **Camembert de répartition en tête du panneau Dépenses** (`category-donut.tsx`) : suit le filtre de mois des pastilles (`?mois=`), total période au centre, liste complète chiffrée €/% à droite. **La couleur suit la catégorie, jamais son rang** (`category-colors.ts`, pur et testé) : gamme de charte verts/olive/doré/oranges — ni rose ni bleu, sur consigne — les sept catégories du plan à teinte fixe, une personnalisée par hachage du slug, donc la même couleur partout et pour toujours, sans colonne en base. Six parts au plus puis « Autres » en gris, « Sans catégorie » en gris aussi — pas des catégories. Palette validée au validateur sur les deux surfaces (≥ 2:1 partout) ; vert et orange restent indissociables pour un daltonien deutan — structurel dans une gamme chaude — et c'est la légende chiffrée qui porte l'identité, jamais la couleur seule. `breakdown.ts` pur et testé range **avec les mêmes règles que le tableau** (`resolveCategory`, manuel d'abord) et n'additionne que les débits EUR — même convention que la carte « Dépensé » |
| Récupération de factures | colonne **Récupération** de `/entreprise/finance`, `src/lib/finance/retrieval.ts`, `scripts/recuperer-factures.ts`, `.github/workflows/recuperer-factures.yml`, `docs/recuperation-factures.md` | 1 table (`finance_retrieval_sources`, 20260902a/b), **une fiche par marchand** — le lien se colle une fois, dans un dialogue ouvert depuis un bouton carré de la ligne, et sert tous les mois. **Le passage tourne sur GitHub Actions**, une fois par jour : ni Vercel (pas de navigateur, rien ne survit d'une exécution à l'autre) ni le Mac. Le runner **rejoue une session** capturée une fois depuis un vrai navigateur (`pnpm factures:connexion <marchand>`), chiffrée sur la fiche avec `FACTURES_SESSION_KEY` — clé distincte de `CREDENTIALS_ENCRYPTION_KEY`, parce qu'elle voyage entre le Mac et le runner, deux endroits qui n'ont pas la clé de prod ; `encryptSecret`/`decryptSecret` prennent désormais un nom de variable en second argument. **Le passage ne se connecte jamais lui-même** : mot de passe, 2FA et captcha ne s'automatisent pas. Il n'agit que le lendemain du prélèvement carte du mois, vu par la synchronisation Airwallex, jamais chaque jour ; après un échec, trois jours d'attente ; le navigateur ne s'ouvre que si une fiche est due. L'envoi réutilise `sendFileToAirwallex` et la boîte Gmail des Reçus. Aucune route HTTP : le script parle à Supabase en direct, comme les autres syncs — les trois routes `/api/finance/invoices/*` de la première version ont été retirées avec leur entrée dans `PUBLIC_PATHS`. « Récupéré » se juge à l'affichage en comparant `auto_retrieved_at` au mois courant UTC ; virements et frais n'ont pas de cellule. **Ni la migration ni le passage n'ont tourné en réel** : tant que la table manque, l'écran montre le bouton lien partout et l'enregistrement répond « Enregistrement refusé ». Deux inconnues à lever au premier passage : l'heuristique de téléchargement (facture / invoice / télécharger / download / .pdf) n'a jamais vu la vraie page Adobe, et **une session ouverte chez soi puis rejouée depuis une IP GitHub peut être refusée** — c'est exactement ce qu'Airwallex fait à Vercel | **Les trois automatismes des Reçus s'accordent aussi hors écran** : le bouton « Toujours » n'apparaît que sur une pièce **en attente**, donc un fournisseur dont toutes les pièces sont traitées n'a aucun chemin d'interface — `pnpm recus:confiance <domaine>` comble ce trou. Attention, `receipt_merchant_rules` a pour clé `(org_id, sender_domain)` et `pipeline.ts` la retrouve par le **seul domaine** : automatiser Superwhisper automatise `stripe.com`, donc tout fournisseur qui facture par Stripe. Les autres gardes tiennent (confiance, plafond, rapprochement obligatoire). Automatisés au 02/09 : grab.com, atlassian.com, finary.com, stripe.com.
| Échéances | `/entreprise/echeances`, `src/lib/billing/` | 2 tables (`billing_engagements`, `billing_installments`), remplaçant du board Monday — **le fond, pas la forme** : flux par statut (À facturer / Facturée triée par émission avec retards en bas / Payée / Devis confirmé) et sommes HT-TTC par groupe, dans des panneaux Antidotes sobres, la couleur vivant dans les `StatusPill` (en-cours orange comme une facture `sent` de Finance, payé vert, planifié bleu, retard rouge). **Chaque groupe est repliable** (`<details>` natif, aucun état client) et plafonné à six lignes visibles, le reste défilant dans le panneau. **La colonne d'état ouvre la ligne**, à gauche du client, sur la même largeur de 8,5 rem du board jusqu'au détail d'un devis — `ROW_LABELS` et `STAGE_TONES` sont exportés de `installment-row.tsx` pour que la même mensualité ne soit pas « Envoyée » en haut de page et « Facturée » dans son devis. Une ligne émise non échue se dit « Envoyée », échue « En retard ». **Une seule colonne Montant** : à 0 % de TVA, HT et TTC affichaient le même chiffre deux fois — la colonne HT est partie de tous les groupes, du détail des devis et des sommes, qui s'alignent sous Montant dans la même grille que les lignes. **Chaque mensualité se supprime** (`deleteInstallment`, confirmation) — depuis le groupe Devis confirmé, où « Supprimer » remplace « Passer » (un mois planifié de trop se retire ; la trace d'un mois offert se pose depuis « À facturer »), et depuis le détail d'un devis signé : même table, la modification se répercute partout au rendu suivant. **Un devis en cours gagne des mensualités** (« Ajouter une mensualité », `addInstallment`) : mois + montant, TVA et devise du devis, la ligne naît « Devis confirmé » et la fenêtre du devis s'étend si le mois en sort (`first_month`/`months_count` suivent les lignes — `lastMonthOf` en dérive la période affichée). Cartes : à facturer, facturé du mois vs prévu aux devis, retards (encre orange), récurrent = moyenne des 12 prochains mois. Courbe prévisionnelle compacte 3/6/12 mois (devis seulement). Un devis se saisit une fois (période, total **ou** mensuel HT, TVA en liste — 0 % défaut) ; **les trois cellules d'une ligne se modifient d'un clic** — période, montant HT, montant TTC ouvrent le même dialogue focalisé sur le champ cliqué, HT et TTC restant d'accord, seul le HT partant au serveur ; **les factures émises hors devis s'affichent aussi** (montant unique HT/TTC, zéro action). Statuts automatiques : bascule « à facturer » dérivée de la date, rapprochement Airwallex (étape `billing` du sync horaire ; relie aussi les payées sans lien et comble les dates manquantes, ne recule jamais). **Il n'y a plus d'archivage** (migration 0028) : une fois payée, une mensualité reste dans Payée, du plus récent au plus ancien. **Le rapprochement passe par `billing_client_aliases`** (migration 0026) : les devis portent la marque — BONDET, CHASSEURS DE GRAINES — et les factures la raison sociale — NIGHT SESSION, MEDIAPILOTE ANGERS ; sans cette table, aucun lien ne se pose et chaque prestation s'affiche deux fois. Un alias sans `client_name` désigne une facture qui n'est pas une prestation client (auto-facturation) : elle reste dans Finance et disparaît des Échéances. Tolérance de montant : 1 %, plafonnée à 5 € — le board portait 2 102,50 là où la facture dit 2 102,00 ; **le rapprochement aligne alors la mensualité sur le montant facturé**, sans quoi le même impayé s'affiche à deux chiffres différents ici et dans Finance. Fenêtre d'émission volontairement étroite (−10 j / +21 j) et **candidate la plus proche du jour prévu**, pas la première venue : à 45 jours elle couvrait le mois suivant en entier, et un devis dont les premiers mois n'ont jamais été facturés voyait toute sa série glisser d'un cran. **Antidotes facture sans TVA** : 0026 remet à 0 les devis repris de Monday, et le fichier d'import ne porte plus 20 %. Trois commandes d'entretien, toutes en étape optionnelle de db-admin : `pnpm diagnostic:billing` (lecture seule), `pnpm dedupe:billing` (devis en double — même client, périodes qui se chevauchent), `pnpm reconcile:billing` (rapprochement à la demande, le cron ne tournant que depuis `main`). **L'historique du board Monday est repris** : `pnpm import:billing-monday` (données figées dans `scripts/data/billing-monday.json`, 30 devis fusionnés par client+projet, statuts des groupes, anti-doublon par couple normalisé, idempotent) — étape optionnelle de db-admin. **Le mois travaillé se choisit sur la carte** : `views` porte une vue par mois — le mois par défaut avec son cycle réel, puis deux mois d'avance. Un mois d'avance n'a pas de fenêtre d'échéance, donc jamais de retard ; la pastille « En avance » et l'encart le disent, sinon quatre segments gris se liraient comme quatre phases en retard. Le choix ne se mémorise pas — un choix d'hier cacherait le retard d'aujourd'hui. Les statistiques des mois d'avance sont lues dans la même requête que le reste. **Le battement d'ambre ne porte jamais sur du texte** : un libellé à 45 % d'opacité tombe à 3,53:1, mesuré au navigateur — c'est la pastille qui bat, le mot se lit. `ofMonth()` élide (« d'octobre »), trois mois sur douze commencent par une voyelle. **Le Contexte sépare contributeur et client** (0040, `app.reaches_context`) : le mot `contributor` était déclaré dans l'enum de 0001 et n'apparaissait dans aucune politique — les deux rôles étaient identiques. Le client garde l'écriture sur le planning, il perd le Contexte ; la Modération reste à faire, **La Modération suit** (0041) : `moderation_clients.workspace_id` existait depuis 0004 et n'était pas emprunté — un contributeur atteint le client de modération rattaché à son espace, et lui seul, sans qu'on duplique de ligne dans `moderation_members` (deux sources de vérité divergeraient au premier retrait de partenaire). **Le logo d'un espace** (0042, bucket privé `workspace-logos`) remplace la pastille de couleur dans le rail et sur la carte : `workspaces.logo_url` existait depuis 0001 et gardait la place ; il porte le **chemin**, jamais l'URL signée — celle-ci expire, et on la signe au rendu en un seul appel pour tous les espaces. Envoi du navigateur droit au bucket, comme les visuels du Planning. Migrations appliquées à la vraie base jusqu'à **0044** (run db-admin 31792209507 du 14/08/2026) — 0045 et 0046 appliquées par l'utilisateur (run db-admin du 17/08), **0047 (`social_posts.media_kind` — reel/carrousel/fixe), 0049 (réseaux à connecter) et 0050 (publication commentée sur `conversations`) attendent le prochain run** : sans 0047, la synchronisation organique échoue sur une colonne absente ; sans 0049, affecter un compte YouTube ou Threads échoue sur une valeur d'enum absente — le reste de l'écran, lui, fonctionne. 0049 n'ajoute que des valeurs d'enum, sans table ni politique : `add value` passe dans la transaction du runner depuis Postgres 12 tant qu'on n'utilise pas la valeur au même endroit, ce qu'elle ne fait pas. Rejouée sur Postgres 16 jetable, deux fois, y compris l'usage après commit. Le compte Meta doit aussi être **rebranché une fois de plus** : `pages_read_user_content` a rejoint les portées après le refus « (#10) » de `/published_posts` au premier sync réel. Rapprochement rejoué au run précédent : 24 mensualités réalignées. Le rapprochement horaire ne tournera en production qu'au merge — le sync s'exécute depuis `main`. **L'écran porte le même badge de synchronisation que Finance**, et déclenche la même chaîne : le rapprochement est l'étape `billing` du sync Airwallex, il n'a pas de collecte à lui |
| Mon travail (phase 1) | `/` (accueil), `src/lib/mon-travail/`, `src/components/mon-travail/` | 3 tables, todo unifiée : « À publier » répliqué des plannings — **le jour même et les retards, rien au-delà** (les retards en rouge d'abord ; une publication du 17 n'est pas du travail du 12, elle arrive le matin venu ; une ligne disparaît en passant `published` ou `dropped`), mêmes cellules éditables, icône de retours comptée et clic visuel qui **renvoient au panneau du post sur son planning** (`?sujet=&focus=retour`), sans coche ni ajout de ligne. Tâches du jour + 4 jours, archivé, ligne quotidienne et cycle mensuel par client générés par `/api/cron/mon-travail`, démo via `pnpm seed:mon-travail`. **Fathom branché sur du réel** : `src/lib/mon-travail/fathom*.ts` lit la section « Prochaines étapes » du compte rendu — française, groupée par personne — et n'en garde que le bloc du propriétaire (`FATHOM_API_KEY`, `X-Api-Key` sur `api.fathom.ai/external/v1`, `include_summary=true`). Les `action_items` de l'API ont été essayés puis écartés : liste exhaustive en anglais, 47 lignes sur 12 réunions. Client déduit du titre, **aucun au moindre doute** — un titre qui en nomme deux n'en désigne aucun. `?purge=fathom` sur la route du cron efface l'import précédent avant de réimporter, pour le jour où la source change. **Les mails sont réels** (`emails.ts` pur et testé + `emails-sync.ts`) : les non-lus de la boîte de réception des trois derniers jours (`in:inbox is:unread category:primary`, plafond 25) deviennent des tâches « Répondre à… », en réutilisant la boîte Gmail des Reçus — même jeton, même chiffrement, aucun second branchement. La règle est celle de la Modération : **ce qui est lu dans Gmail est traité** et ne revient jamais ; expéditeurs automatiques écartés et comptés, client déduit de l'objet et de l'expéditeur avec la prudence Fathom, idempotence par identifiant de message. `?purge=email` efface les tâches du seed de démonstration. Sans boîte connectée, l'étape le dit et le reste du cron continue. **Les publications validées restées derrière** (statut prêt, date passée) s'affichent en bandeau ambre avec un bouton qui les replanifie à aujourd'hui |
| Contexte client | `/espace/[workspace]/contexte`, `src/lib/context/` | 3 tables (`client_context` versionné, `client_assets`, `wording_history`) + bucket privé `client-assets`, **owner-only de bout en bout** : lien de nav non rendu aux clients, page en 404, RLS `app.owns_workspace` (« agency » ≡ owner d'organisation). Brief rangé en **trois familles** sous leur `SectionHeader` — la marque (contexte principal en 7 colonnes, positionnement et cibles empilés à côté), le contenu (livrables, piliers), la parole (ton, mentions, interdits, plateformes) — édition en place au blur, piliers structurés, règles par plateforme, compteur de contexte injecté (encre warning au-delà de 6 000 tokens). **Livrables mensuels** (0034, 0038, 0039 — colonne `deliverables` jsonb à forme complète par défaut) : **un groupe par réseau du client**, chacun avec ses catégories de publication et leurs quantités, plus la date de livraison des intentions. Le volume se compte réseau par réseau parce qu'il se contracte comme ça ; le total global reste en tête de panneau et en carte de mesure. Les réseaux se déclarent là et **commandent les rangées des règles par plateforme**, qui proposaient les quatre mêmes à tout le monde ; une règle déjà écrite sur un réseau retiré survit, une clé restée vide non. 0039 convertit l'ancienne forme (réseaux en simples noms, volume global) sans rien inventer : le volume d'avant devient un bloc « hors réseau », visible et déplaçable à la main, plutôt qu'une répartition devinée. Décocher un réseau qui porte des quantités demande confirmation — **contractuels, donc jamais proposés par la consolidation**, et reportés tels quels au versionnage. Le numéro de version a quitté l'en-tête : l'historique est un panneau en bas de page (consulter, restaurer), et la première carte de mesure dit le volume dû chaque mois. Documents en glisser-déposer **du navigateur droit au bucket** (URL d'envoi signées, même mécanique que les visuels du Planning — jamais par une action serveur), extraction `claude-sonnet-4-6` par type de document via `/api/contexte/extraction` (texte extrait en local par unpdf/mammoth, vision base64 pour images, lookbooks et PDF scannés, découpe des documents longs), un résumé retouché à la main n'est écrasé que sur relance explicite. Régénérer = consolidation en sortie structurée + **diff champ par champ** acceptable/refusable + version + 1 active, l'ancienne restaurable — jamais d'écrasement. `getClientContext()` (`src/lib/context/get-client-context.ts`) sert les futures routes `/api/generate/*` de la branche cartes-client ; colonnes `visual_text`/`slides`/`wording_status` sur `planning_subjects`, l'accroche part dans `wording_history` **à la validation** du wording, pas à la génération. Sans `ANTHROPIC_API_KEY` : lignes en `error` avec la cause affichée, rien d'inventé |
| Administration d'un espace | trois points du rail, `src/lib/workspaces/`, `src/app/actions/workspaces.ts` | 1 table (`workspace_page_grants`, 0035/0036) + helper `app.current_email()`. Renommer (le nom seul, **le slug ne bouge pas** : les liens partagés survivent), **ouvrir un client à partir d'un autre** (l'ancien « Dupliquer », `duplicate-dialog.tsx`) : la configuration est copiée, **jamais le contenu** — tableaux avec leurs colonnes et leur vocabulaire, tableaux de bord ; ni publication, ni document, ni brief. Le dialogue demande en plus **logo, année et réseaux avec leurs livrables mensuels**, et cette déclaration unique sert trois fois : livrables du Contexte, **douze mois vides** de l'année avec un couloir par réseau dans chacun (`src/lib/workspaces/setup.ts`, pur et testé), et lignes de l'écran des connexions. C'était quatre saisies de la même chose, qui divergeaient. Le logo part **après** la création, le chemin du bucket contenant l'identifiant de l'espace ; un envoi raté n'annule rien et le dit. Les mois ne sont posés que sur un tableau `editorial` — une FAQ n'a pas de calendrier. Supprimer (nom exact à saisir, vidage des deux buckets avant la ligne, journalisé), et le tableau des partenaires : invitation par adresse, rôle, et une case par page. `listWorkspacePages()` est la **source unique** du menu de l'espace, de la porte d'entrée et de la matrice de droits, sinon un onglet mène à un 404. Garde `requirePageAccess()` en 404 sur chaque page. Rejoué sur Postgres jetable : renommage, partenaire avec page masquée, suppression refusée sur nom faux puis effective. **Ouverture de client rejouée au navigateur** contre les 52 migrations appliquées — 12 mois sur le tableau éditorial, 0 sur la FAQ, 36 couloirs pour 3 réseaux, contexte rempli, **0 publication** |
| Production (cartes client) | cartes de `/`, `src/lib/production/`, `src/lib/prompts/*.md`, `/api/generate/[phase]`, `/api/jobs/[id]` | 3 tables (`client_phases`, `generation_jobs`, `wording_history` — le « client » du cahier des charges est un espace : `workspace_id` + `org_id`, RLS owner-only). Cycle décalé à 4 phases : pendant le mois M, intentions/wording/programmation visent M+1 et reporting M−1 — le cahier des charges faisait viser « le mois courant » à wording/programmation, incompatible avec ses propres fenêtres ; bascule reporting → intentions à la génération ou le 15. Carte cockpit : 4 segments (vert = fait ou en cours, ambre = fenêtre ouverte ou dépassée, gris = pas encore), badge du retard le plus ancien, mesures par phase, avancement chiffré, encart d'alerte, bouton à compteur, menu `…` (4 actions + liens, dont « Ouvrir le contexte » qui pointe la page du module Contexte). Jobs en `claude-sonnet-4-6` (demandé explicitement — les autres modules restent en opus) depuis des **prompts markdown éditables sans code**, tracés pour Vercel dans `next.config.ts` ; wording par lots de 4, reprise des seuls échecs, anti-répétition via `wording_history` ; intentions insérées dans le planning (mois, réseaux, colonne « Intention » — jamais dans `wording`, qui reste la caption et le compteur). **Les intentions complètent un mois entamé, elles ne le refont pas** : `quotas.ts` (pur, testé) rapproche les livrables du Contexte — volume par réseau et par catégorie — des lignes déjà posées, et le prompt reçoit un « TOTAL À CRÉER » qui fait autorité ; contrat déjà honoré ⇒ le job répond « rien à ajouter » sans appeler le modèle. Les deux vocabulaires ne sont pas les mêmes (catégorie en texte libre contre enum de format, « Instagram » contre le réseau META du board) : ce qui ne se rapproche pas est **dit** dans le décompte, jamais imputé au hasard, et le nom exact d'un réseau sert avant le parapluie META. Les intentions reçoivent aussi les accroches déjà publiées — reprendre un produit, oui ; reprendre la manière d'en parler, non. **La phase Content lit le brief laissé dans la cellule Wording** : `wording-state.ts` porte le prédicat `needsContent`, partagé exprès entre le worker et le compteur du bouton pour qu'ils ne divergent jamais — un sujet en « — » ou « WORDING À FAIRE » se réécrit brief compris, un sujet à valider ou au-delà ne se touche pas, un publié jamais. **La phase Content ne rédige plus que la caption** (31/08) : le contenu de créa — déroulé, texte visuel, slides — se fige à la phase Intentions, dans la colonne Intention, parce que c'est lui qui part en validation ; l'empiler derrière la caption fusionnait les deux phases et rajoutait du travail (vécu sur Bondet septembre). Le prompt de wording reçoit désormais les **derniers wordings validés** (`previous-wordings.ts`, pur et testé : `wording_history` puis les cellules des mois antérieurs aux statuts validés — l'historique importé de Monday vit là, inutile d'appeler l'API Monday) : registre de ton à suivre puisque validé par le client, formules interdites de reprise, et sans aucun précédent le modèle avance sur le seul brief ; la forme est cadrée en dur — court, direct, ni didactique ni poétique. **Une story n'a toujours pas de légende** : la cellule reçoit le texte écran par écran, et rien n'entre dans `wording_history` (ce ne sont pas des accroches publiées) ; reporting markdown sur les volumes du planning (pas de chiffres de régies : il le dit) ; **programmation en stub** qui journalise sans rien clôturer. Traitement post-réponse par `after()`, poll 3 s, jobs muets > 10 min soldés en erreur. **Un job en cours s'arrête depuis la carte** (`cancelled`, migration 0034) : un job actif tient le bouton de son espace, et sans cette sortie la seule issue était d'attendre les dix minutes de la purge. `POST /api/jobs/[id]/annuler` écrit le statut sous condition `in ('pending','running')` — un verdict arrivé pendant le clic gagne — et le worker relit ce statut entre deux lots de wording (le lot en vol va au bout : rien n'annule une requête déjà partie chez le modèle) puis avant d'écrire son propre verdict, sinon une phase à appel unique écraserait `cancelled` par `done`. Un arrêt n'est pas un échec : la carte repropose l'action normale, pas la reprise en ton danger, et la phase n'est pas clôturée. Sans les tables, la carte dit « Cycle indisponible » et retire son bouton plutôt que d'affirmer un retard qu'elle ne sait pas — l'erreur Supabase (`42P01`/`PGRST205`) est testée, contrairement à la règle habituelle des `queries.ts`. Migrations 0032-0033 appliquées à la vraie base ; **0034 reste à appliquer** (une ligne, `alter type generation_job_status add value if not exists 'cancelled'`), sans quoi l'arrêt répond 500. `supabase/seeds/rattrapage-production.sql` pose l'ensemble d'un seul collage, convergent (il répare un passage interrompu, refuse de toucher une table peuplée d'une autre forme) et porte désormais 0034. **La phase Programmation est devenue « Envoyer en validation »** : plus rien à programmer — la publication est automatique à 16h — donc le moment du cycle est celui du client. Le bouton envoie, depuis la boîte Gmail des Reçus, « le planning du mois de {mois} est disponible pour review » aux partenaires invités de l'espace (`sendPlanningValidation`, route owner-only 404), et la phase ne se clôt que si au moins un courriel est parti. Le segment s'affiche « Validation », la clé en base reste `programmation`, `/api/generate/programmation` (stub) existe toujours mais n'est plus appelé par la carte. **Une action impossible ne se lance plus en silence** : le modèle porte un `menu` — les quatre phases avec leurs gardes — là où le menu `…` lançait n'importe quelle phase sans regarder si elle avait de la matière. Un espace arrivé en cours de route n'a pas de planning le mois d'avant : « Générer le reporting » créait un job qui échouait à la première ligne, et l'écran donnait l'impression que le clic n'avait rien fait. Désormais l'entrée est grisée, **la raison s'affiche dans l'encart de la carte** (elle vivait dans un `title`, invisible au clic), et un clic sur une action bloquée répond par un toast qui la nomme. `launch()` tolère un corps vide — un 404 de module interne faisait remonter une erreur de syntaxe JSON à la place de la cause. **Les segments se cliquent** : `POST /api/production/phases` pose l'état à la main — terminée, passée, ou à faire (la ligne est supprimée, l'absence de ligne *étant* l'état neutre). C'est la seule sortie d'une phase qui ne peut pas se dérouler ; « en retard » ne s'y choisit pas, il se déduit de la fenêtre, et le menu le dit. **La carte est une colonne à hauteur pleine** : en-tête à deux lignes fixes, retard descendu dans la rangée d'état, pied poussé par `mt-auto` — deux clients aux informations inégales gardent barres et boutons à la même altitude (Δ 0 px, mesuré au navigateur en clair et en sombre). « Ouvrir le reporting » vise enfin le tableau de bord (`listReportingSlugs`, une requête pour toutes les cartes) : il pointait `/espace/[slug]`, qui n'est qu'une porte redirigeant vers la première page — et le planning passe avant le reporting. **Le compte rendu existe enfin quelque part** (0056/0057, `client_reports`) : il partait dans `generation_jobs.result.report`, qu'**aucun écran ne lisait** — la phase se clôturait sur un livrable invisible. Il vit désormais dans sa propre table, un rapport par espace et par mois, et s'affiche en tête de la page Reporting du mois analysé, replié, avec sa première phrase en aperçu. **Interne, jamais client** : écrit par un modèle, il sert à préparer le point mensuel — RLS owner-only comme le reste du module, panneau rendu au seul propriétaire, et absent d'une plage libre (un bilan mensuel n'appartient pas à quinze jours). **Et il lit enfin les vrais chiffres** (`reporting-facts.ts`, pur et testé) : dépense, portée, achats, CPA, ROAS, engagement, abonnés, tops — les mêmes lectures que l'écran, repli des événements pixel compris, **variations calculées côté code** et non laissées à l'arithmétique du modèle. Le prompt disait littéralement que « les données de régie ne sont pas encore branchées », ce qui était vrai à l'écriture et faux depuis le connecteur Meta. Les volumes du planning restent, mais comme complément : ils disent ce qui était **prévu**, ce qu'aucune régie ne sait. Corollaire, la garde a changé de nature — elle portait sur l'existence d'un mois de planning, elle porte sur l'existence de **quelque chose** : un client arrivé en cours de route, avec des chiffres Meta et sans tableau, a droit à son bilan. `« — »` reste `« — »` et n'est jamais présenté comme une contre-performance |

| Academy | `/academy`, `/academy/admin`, `src/lib/academy/` | 6 tables (0056/0057 + 20260903a-c) + deux buckets privés. **Un espace de formation type Skool, à plusieurs formations** : « Devenir freelance social media manager » (13 modules, 54 leçons) et « Devenir libre grâce à l'UGC » (14 modules, 65 leçons, 71 600 mots, 63 documents de travail lisibles dans la leçon). La lecture descend d'un cran — `/academy/[formation]/[module]/[lecon]`. **Trois profils, et c'est la nouveauté structurante** : l'owner administre, un membre de l'organisation suit tout, et une **élève** — quelqu'un qui a acheté une formation — ne voit **que les formations où elle est inscrite**, et **rien d'autre dans Antidotes** : son rail n'a qu'une entrée, `requireOwner` la renvoie sur ses formations plutôt que sur un hub vide, et les modules internes gardent leur 404. `academy_enrollments` porte l'inscription **par adresse** — elle vaut avant que le compte existe — et `app.handle_new_user()` la raccroche à la première connexion **sans jamais poser de ligne dans `organization_members`** : l'élève entre dans la formation, pas dans l'entreprise. La RLS de 0057 est réécrite : « membre **ou inscrite à cette formation** ⇒ lit le publié **de cette formation** », la chaîne du publié inchangée, plus deux helpers `app.academy_course_ids()` et `app.academy_org_ids()` — la seconde parce que sans elle une élève ne pourrait pas cocher une leçon terminée. **Inscription et courriel d'arrivée** depuis `/academy/admin/membres` : `auth.admin.generateLink` fabrique le lien, Resend le poste (HTTP direct, aucune dépendance, 3 000 messages/mois gratuits) ; **sans `RESEND_API_KEY` l'inscription est quand même écrite et le lien s'affiche à copier** — dégradation prévue, pas subie. Le lien passe par `token_hash`, la seule forme qui marche quand on l'ouvre ailleurs que dans le navigateur qui l'a demandé. **`/mon-profil`** est le seul écran de réglage ouvert à tout le monde, élèves comprises — photo, prénom, nom, envoi du navigateur droit au bucket `member-avatars`. **Miniatures d'introduction** sur les formations et les modules (`cover_url`, bucket `academy-assets`) ; sans image, un dégradé dérivé du slug — stable, donc jamais clignotant — dont le contraste du blanc a été calculé sur les 72 teintes : **6,84:1 au pire**. Le parseur markdown maison gagne les **tableaux**, sans quoi une grille tarifaire se rendait en paragraphe de tuyaux. Contenu dans `scripts/data/academy/` et `scripts/data/academy-ugc/`, `pnpm generate:academy-seed` fabrique les deux migrations — **`0058` est appliquée et se régénère à l'octet près** (`body` n'entre dans le jsonb que sur les ressources qui en portent un, sinon son empreinte changeait). `20260903a_academy_ugc_seed.sql` **rejouée sur Postgres 16 jetable** : 14 modules, 65 leçons, 63 documents. **20260903a-c ne sont pas encore appliquées à la vraie base** — elles partiront au merge (job `migrer`), et `tests/academy-isolation.test.ts`, étendu aux quatre postures de l'élève, ne pourra tourner vert qu'ensuite. Les 54 + 65 vidéos restent à tourner |
| Partage public du Reporting | `/partage/[token]`, `src/app/actions/partage.ts`, `share-button.tsx` | La table `share_links` sert enfin : « Partager » (owner seulement) crée un jeton de 64 hexdigits, période **figée** à la création, lien copié au presse-papier. La page publique résout le jeton en admin (chemin déjà dans `PUBLIC_PATHS`), 404 si révoqué ou expiré, et rend les **mêmes composants** que le dashboard interne — onglets limités aux réseaux qui portent des chiffres, aucune synthèse interne, aucun levier. Les lectures acceptent un `reader` injecté (`getAdsData`/`getOrganicData`/`getWebData`), ce qui survivra à la fermeture de l'accès. « PDF » = impression du navigateur avec feuille `@media print` (`print-cacher`, cartes à plat) — pas de rendu serveur, hors free tier. `robots: noindex` |

**Les onglets du Reporting suivent le contrat, plus les seuls comptes branchés.** Ils n'apparaissaient qu'une fois un compte affecté : un client vendu sur Meta ouvrait un Reporting vide qui ne disait pas qu'il restait un geste à faire. Un onglet déclaré au Contexte s'ouvre maintenant même sans compte, et son état vide dit lequel affecter et où — `networksFromContextName("Meta")` en ouvre trois, tandis que « Meta Ads » n'ouvre que le payant (le test `\bmeta\b` vient en dernier pour que l'un ne mange pas l'autre). Les réseaux qu'aucun connecteur ne sert — TikTok — ressortent dans `sansConnecteur` et sont **nommés** à l'écran : sans ça le Reporting d'I-WAY était parfaitement muet, ce qui se lit comme une panne alors que c'est une fonctionnalité absente. LinkedIn a désormais le sien (`docs/linkedin-connexion.md`). Démarches TikTok dans `docs/connecteurs-linkedin-tiktok.md` — entité juridique et politique de confidentialité exigées côté LinkedIn, audit d'interface côté TikTok ; ce sont les validations qui prennent des semaines, pas le code.

**Synchronisation à la demande : ce sont les écrans qui déclenchent Airwallex.** Le bandeau de Finance annonçait « mise à jour chaque heure » ; un mardi à 11 h 14, les chiffres dataient de 5 h. Rien n'était cassé — le `cron: "17 * * * *"` d'`airwallex-sync.yml` est une **intention**, et GitHub en laisse tomber près d'une exécution sur deux (relevé le 18/08/2026 : 13 passages le 17 au lieu de 24, des trous de 2 h 30 en journée, 6 h d'affilée le matin du 18). Ouvrir `/entreprise/finance` ou `/entreprise/echeances` lance donc désormais un `workflow_dispatch` sur ce même workflow, qui met les **deux** écrans à jour d'un coup — le rapprochement des Échéances est l'étape `billing` du sync Finance. Le travail ne part toujours pas de l'hébergeur (Airwallex refuse ses IP) : `src/lib/finance/github-actions.ts` ne fait que donner l'ordre, ce que Vercel sait faire. Trois gardes, dans cet ordre — indisponible (jeton absent ou refus GitHub), exécution déjà en vol, fenêtre de fraîcheur de dix minutes ; `sync-state.ts` est pur et testé, et le serveur tranche seul, car un autre onglet peut très bien venir de lancer la collecte. `POST /api/finance/sync` déclenche, `GET` rend l'état, le badge sonde toutes les 6 s et `router.refresh()` relit la page quand l'exécution s'achève. Le bouton « Synchroniser » passe outre la fraîcheur, jamais une course en cours. **Il faut `GITHUB_SYNC_TOKEN` sur Vercel** (jeton à portée fine, *Actions : read and write* sur ce seul dépôt) : sans lui, le fond horaire continue et l'écran écrit « relance indisponible » avec la cause en infobulle, plutôt qu'un bouton mort. Le cycle complet — déclenchement, sondage, fin, re-rendu — a été rejoué au navigateur contre une façade GitHub locale (`GITHUB_API_BASE`), audit de contraste sans violation nouvelle sur les deux pages. Et contre le vrai GitHub : dispatch accepté en 204, exécution 32097669625 verte en **56 s**. **Depuis le 31/08, l'écran ne déclenche plus que la portée `finance`** : la chaîne complète avait grossi jusqu'à 4-6 min 30 par passage — navigateur de rendu, Modération (escaliers de volume Meta), Reçus — et le témoin « Synchronisation en cours… » durait tout ça pour des chiffres qui n'attendaient que la collecte Airwallex. Le `workflow_dispatch` porte un input `portee` (`tout` par défaut, ce que gardent le passage horaire — `inputs.portee` y est vide — et le bouton « Run workflow » de GitHub) ; l'application envoie `finance`, qui saute navigateur, Planning, Modération et Reçus : checkout, install, `sync:finance`, ~1 min. Corollaire : le bouton « Synchroniser » des écrans ne relève plus les Reçus — leur cadence est le passage horaire, et c'est voulu.

**Tous les clients ne vendent pas en ligne** (0051/0052, `ad_custom_events_daily`). Le Reporting ne connaissait que les événements standards de Meta, or I-WAY optimise sur « Validation Shop Lyon » — un événement que son pixel émet sous un nom à lui, rendu par Meta sous `offsite_conversion.fb_pixel_custom.<Nom>`. Son Gestionnaire affichait « 2 validations à 49,93 € » quand Antidotes affichait « 0 achat » : pas un désaccord de chiffres, deux grandeurs différentes. `customEvents()` les relève avec leur nom — coupé **après le préfixe** et non au dernier point, sinon « Résa 2.0 » devient « 0 ». Ils ne rejoignent **jamais** `purchases` : une validation de boutique n'est pas une vente, et les y verser fabriquerait un ROAS à partir d'un événement sans montant. Ils s'affichent dans leur propre panneau, sous leur nom, avec un coût par conversion **recalculé** depuis la dépense de la période. Piège vérifié sur le vrai compte : ce ne sont **pas** des « conversions personnalisées » au sens de Meta — `ads_get_customconversions` rend une liste vide — mais des événements pixel personnalisés, ce qui change la façon de les collecter. Le panneau ne s'affiche pas du tout quand le client n'en a aucun.

**…mais chez certains, c'est bien la vente** (0053/0054, `data_sources.purchase_event_names` et `add_to_cart_event_names`). Le défaut ci-dessus reste le bon pour tout le monde sauf I-WAY, dont le pixel n'émet **que** des événements à lui : « Validation Shop Lyon » et « Validation Shop Paris » sont les ventes, « Validation Resa Lyon » et « Validation Resa Paris » les mises au panier. Un seul rôle ne suffisait donc pas — tout verser dans les achats aurait doublé le chiffre. Chaque événement reçoit un rôle **achat / panier / aucun**, réservé à l'owner, et le réglage d'I-WAY est **posé par la migration** plutôt que laissé à un clic. Ce qui s'est décidé là :

- **Ne jamais filtrer sur un préfixe unique.** `customEvents()` ne gardait que `offsite_conversion.fb_pixel_custom.*` — et I-WAY n'a **jamais rien remonté**, deux synchronisations de suite, sur juin comme sur juillet. Elle garde maintenant **tout ce qui n'est pas une métrique standard** (liste écrite en toutes lettres, variantes de source comprises, sinon l'achat s'afficherait deux fois). Un type inconnu apparaît sous sa forme brute : ça se voit et ça se corrige, là où le silence se lit « ce client n'a aucune conversion ».
- **L'API Insights ne rend pas les noms** (constaté au premier vrai sync, 0055). Le Gestionnaire affiche « Validation Shop Lyon » colonne par colonne, mais `actions` ne porte que l'agrégat `offsite_conversion.fb_pixel_custom` — 11 conversions et **466,30 € de valeur** sur juin, le même champ que le « Website custom conversions » du Looker de référence (Purchase 11, Earn 466,3). Deux corollaires : ces événements **portent un montant**, donc CA et ROAS vivent (466,30 / 572,29 = 0,81 — le 0,94 du Looker vient du champ `purchase_roas` de Meta, autre attribution) ; et le rôle d'achat d'I-WAY est posé **sur l'agrégat**, les quatre noms restant configurés pour le jour où Meta les rendrait. Ce jour-là, le collecteur écarte l'agrégat d'une ligne qui porte les noms — il en est la somme — et la synchronisation **remplace la fenêtre au lieu de s'y empiler** (le nom fait partie de la clé : un upsert seul laisserait l'agrégat d'hier à côté des noms de demain, et le fold compterait les deux).
- **« Tout ce qui n'est pas standard » ramassait l'engagement.** Huit cartes de likes et de sauvegardes (`post_interaction_*`, `onsite_conversion.post_*`) au premier sync réel — toutes redites de métriques déjà affichées. Exclues nommément (`TYPES_BRUIT`/`PREFIXES_BRUIT`), purgées de la base par 0055, et l'agrégat s'affiche sous le libellé « Conversions du pixel (total) » plutôt que sous son nom brut. **Le bruit ne s'exclut que sur preuve** : `onsite_conversion.messaging_*` n'y entre pas — `messaging_conversation_started_7d` est LA conversion d'une campagne click-to-Messenger, et l'exclure d'avance referait pour le prochain client le silence qu'on vient de réparer.
- **La comparaison se plie comme la période.** `previousTotal` passait à côté du repli : chez un client dont tous les achats viennent du pixel, chaque mois aurait affiché « M-1 : 0 » et des deltas infinis. La fenêtre des événements couvre désormais la période **et** sa comparaison, et les deux totaux passent par `foldClientConversions` — vérifié à l'écran : 11 vs 8 = +37,5 %, CPA −36,6 %, CA +55,4 %.
- **Ce ne sont ni des conversions personnalisées, ni une devinette.** `ads_get_customconversions` rend une liste vide sur le compte `837962101192463` **et** sur son dataset `1895820983835692`. Le compteur du dataset, lui, montre les quatre noms qui tournent en continu. C'est la seule source qui ait tranché — la relecture de code ne l'aurait jamais fait. Même leçon au tour suivant : c'est le **résultat du sync en production** qui a montré l'agrégat sans noms et le bruit d'engagement.
- **Le repli se fait à la lecture, jamais à la collecte** (`foldClientConversions`, `src/lib/reporting/real-data.ts`). Les lignes restent fidèles à Meta ; changer un rôle prend effet au rafraîchissement suivant, sans resynchroniser un an d'historique. Un événement n'a **qu'un** rôle : l'action le retire des deux listes avant de le remettre dans la bonne, sans quoi passer d'achat à panier le compterait deux fois.
- **Le réglage vit sur `data_sources`, pas sur `workspaces`** : c'est une façon de lire *un compte publicitaire*. La politique `data_sources_write` de 0002 le protège déjà — rejouée sur un Postgres 16 jetable dans les deux sens : un membre non-owner **lit** la connexion mais son `update` touche 0 ligne, celui de l'owner en touche 1. Deux cas dans `tests/isolation.test.ts`.
- **Le chiffre d'affaires ne suit pas.** Les quatre événements d'I-WAY ne portent aucun montant : achats, paniers et CPA deviennent justes, CA et ROAS restent à zéro, et le panneau le dit. Inventer une valeur d'achat serait exactement la faute qu'on s'interdit. Le rapprochement des noms est insensible à la casse et aux accents, sinon « Résa » et « Resa » désigneraient deux réglages.
- **Le réglage est un menu dans la barre de page, pas un panneau.** Il a d'abord occupé une bande pleine largeur : le poids visuel annonçait « voici le sujet de cet écran » pour un réglage qu'on touche deux fois par an, chez un client sur cinq. C'est devenu `ConversionsMenu`, un bouton de 32 px à côté de Synchroniser, qui n'existe ni pour un client ni pour un compte sans événement personnalisé. Deux pièges au passage, tous deux déjà payés ailleurs dans le dépôt : `DropdownMenuLabel` est un `Menu.GroupLabel` que Base UI **exige dans un `Menu.Group`** — posé seul il jette l'erreur #31 et le menu s'ouvre **vide sans rien casser d'autre**, donc invisible hors du navigateur ; et un troisième bouton dans la barre la faisait déborder à **467 px pour 390** sur téléphone, `shrink-0` empêchant l'ajustement sans autoriser le retour à la ligne.
- **Le sélecteur actif est en `primary`, pas en `accent-ink`.** En sombre, l'encre d'accent devient un vert clair et le blanc posé dessus tombe à **1,39:1** — relevé à l'audit, pas à la relecture.
- **Un seul rapprochement de noms, partout.** La lecture pliait (casse, accents) quand l'écriture retirait en strict et que la carte jugeait en strict : « Résa » posé à la main face à « Resa » relevé pouvait compter le même événement **dans les deux rôles**, ou afficher « Ni l'un ni l'autre » sur une carte pourtant comptée. `foldEventName` et `conversionRoleOf` (exportés de `real-data.ts`) sont désormais l'unique juge — lecture, Server Action et panneau — et le fold est **exclusif**, l'achat gagnant sur le panier. Trouvé par revue adversariale, pas à la relecture.

Vérifié au navigateur sur les données que la production porte réellement — l'agrégat à 11 / 466,30 € plus le bruit : **11 achats, CPA 52,03 €, CA 466,30 €, ROAS 0,81**, une seule carte proprement libellée avec « Achat » présélectionné, zéro carte de bruit, et le tableau par ad set qui suit. Les paniers restent à 0 tant que Meta ne rend pas les noms — les Resa sont configurés, pas mesurables séparément.

**Une demande trop large se découpe, elle ne se rejoue pas telle quelle.** Meta refuse par « Please reduce the amount of data you're asking for » — et le déclencheur n'est pas la durée seule mais **durée × ventilation** : les Insights par région au grain jour rendent une ligne par région et par jour, et le rattrapage initial en demande douze mois. Le compte publicitaire d'I-WAY est tombé dessus au premier passage, ce qui laissait tout le Reporting payant vide. `src/lib/connectors/meta/windows.ts` (pur, testé) essaie la fenêtre entière **d'abord** — le cas courant reste un seul appel — puis redescend par paliers 31 j / 7 j / 1 j sur ce refus précis, et **sur lui seul** : se découper devant un jeton expiré ferait 365 appels pour le même refus. Les tranches partent **en série** — échanger un refus de volume contre un refus de débit ne réglerait rien — et leurs bornes sont inclusives des deux côtés, sinon un jour tombe entre deux tranches et le trou ne se voit que sur la courbe, des semaines plus tard.

**Zéro publication n'est pas une mesure à zéro.** L'organique affichait six tuiles à « 0 » quand aucune publication n'était lue — ce qui se lit comme une contre-performance du client, alors que rien n'a été mesuré. Elles rendent « — » et le delta disparaît (`organic-dashboard.tsx`) : la règle de la maison vaut ici comme ailleurs. **Sur Facebook, les publications se lisent** depuis le rebranchement du 30/08 avec `pages_read_user_content` (vérifié sur I-WAY le 2/09 — l'ancienne note « App Review obligatoire » était fausse pour un compte à rôle dans l'app). Les impressions et la portée par publication sont **redemandées** (`post_impressions`, `post_impressions_unique`) : ce que Meta refuse manque seul, le repli de `fetchPostInsights` est métrique par métrique, jamais en bloc. Le panneau ne parle d'App Review que lorsqu'aucune publication n'a été lue. **Et quand Meta refuse les impressions par publication, la Page les donne** (0069/0070, `social_page_daily`) : `fetchPageInsights` lit `page_impressions`, `page_impressions_unique`, `page_post_engagements`, `page_video_views` au grain jour — tranches de 90 jours, métrique par métrique en repli — et `pageInsightsToDaily` date chaque point **du jour qu'il clôture** (`end_time` est 07:00 UTC le lendemain). À la lecture, `withPageFallback` remplace une somme de posts à zéro par la somme de la Page, sur la période et sa comparaison. Test d'isolation `tests/page-daily-isolation.test.ts`, **vert contre la vraie base** le 2/09 (run db-admin 33628335578).

**L'antériorité des abonnés se reprend à la main, une fois.** Meta ne rend pas l'historique d'abonnés, seulement le compte du jour : sans reprise, chaque client repart de zéro le jour de sa bascule et on perd un an de courbe pour toujours. `pnpm import:followers` (`--dry-run` pour voir sans écrire) — **étape « Antériorité des abonnés » de `db-admin`**, seul moyen de la jouer contre la vraie base, comme toutes les opérations de schéma — lit `scripts/data/followers-anteriorite.json` — chiffres relevés sur les tableaux Looker, relisibles et corrigeables sans toucher au script — et pose un relevé par mois au 1er, avec **`source = 'looker'`** et jamais `'api'` : l'origine d'un point reste lisible pour toujours. Il réutilise la source de données du connecteur quand elle existe, si bien qu'un relevé d'API postérieur écrase naturellement une recopie du même jour — la mesure gagne sur la saisie. Il retrouve l'espace par **slug ou nom normalisés** (`src/lib/workspaces/lookup.ts`, testé) : le fichier nomme les clients comme on les dit à l'oral, et le slug de la base n'en découle pas — deviner « i-way » a fait tomber 28 relevés sur 32. Et un espace introuvable **fait échouer le passage** en listant les espaces connus : la première fois il s'était contenté d'un avertissement, le workflow était resté vert en jetant les trois quarts des données. Une perte silencieuse ne doit pas ressembler à un succès. Il se rattache à la source du **compte réellement affecté** dans les Connexions et non à la première source du même `provider` : `meta_organic` couvre à la fois le compte Instagram et la Page Facebook, et prendre la plus ancienne aurait posé l'histoire Instagram sur la source de la Page — l'API écrivant ailleurs, la courbe aurait porté deux points pour la même date. Idempotent par la clé `(data_source_id, platform, date)`. **Le tableau « Meta » du Looker porte les abonnés Instagram**, pas Facebook : I-WAY y affiche 21 742 en juillet 2026 quand son onglet Facebook en compte 295.

**Un relevé d'abonnés porte la date du jour qu'il clôture, jamais celle du passage.** Le cron lit le compte à 5 h UTC : ce qu'il lit le 1er septembre est le nombre d'abonnés au sortir du 31 août — le point de clôture d'août. Daté du 1er, il tombait sous « septembre » dans la courbe et dans le tableau, chaque client avait un mois d'avance, et le reporting d'août ne portait pas son propre chiffre (relevé le 2 septembre 2026, sur tous les clients). La règle vit dans `closingDate()` du connecteur Meta (`date = veille du passage`) et la migration 0067 a reculé d'un jour tous les relevés d'API existants — les reprises Looker, datées du dernier jour du mois qu'elles décrivent, n'ont pas bougé. **Tout connecteur d'abonnés à venir (LinkedIn, TikTok, YouTube) suit la même règle** : un instantané se date du jour qu'il ferme, et `monthlyFollowersSeries` prend le dernier relevé de chaque mois civil sans rien décaler — c'est à l'écriture que la date est juste, pas à la lecture. **Le mois en cours ne s'affiche jamais** : le 2 septembre, la courbe s'arrête à août ; septembre apparaît le 1er octobre, avec son point de clôture. Le chiffre du jour vit dans la phrase du héros.

**Vues vidéo et vues à 100 % sur le payant** (0066, `ad_metrics_daily.video_views` / `video_completions`) : `video_view` dans `actions` est la vue au sens de Meta (3 s), la lecture complète arrive dans `video_p100_watched_actions`, même forme, même `action_type` — c'est le champ qui porte le sens (`mapping.ts`, testé). Deux tuiles de plus sur Meta Ads, LinkedIn Ads et TikTok Ads ; le taux de complétion se lit en rapprochant les deux, jamais stocké. **`video_view` n'est pas dans `actions`** — le catalogue Meta ne le connaît pas comme action, et le premier sync a affiché 0 sur 255 000 impressions vidéo : le champ canonique des vues est `video_play_actions`, demandé explicitement, `actions` ne restant qu'en repli. La tuile Achats est partie (le chiffre vit dans le CPA, l'entonnoir et le tableau) et le ROAS tient sur une case : 1 + 11 = trois rangées de quatre. **Un mois sans dépense garde son écran** quand le précédent en avait : « −100 % » et des « — », pas un état vide — I-WAY n'a rien diffusé en août 2026 (vérifié chez Meta : juin 572 €, juillet 373 €, août 0 €). L'organique n'a pas de lecture complète : Instagram ne la rend pas, Meta a retiré les métriques vidéo de Page — pas de tuile qui promettrait un chiffre. **Facebook porte la même grille qu'Instagram** (impressions, enregistrements) à la demande du client, mais Meta ne rend ni l'une ni l'autre sur une Page : `isUnmeasuredZero` écrit « — » à la place du zéro, sans variation, et la tuile vivra sans code le jour où la source les rend. La colonne « Vues » du tableau des ad sets s'appelle **Impressions** : c'est ce qu'elle compte, et « vues » se lisait comme des personnes.

**LinkedIn Ads et TikTok Ads sont des onglets à part** (0068, `linkedin_ad_account` / `tiktok_ad_account`), comme Meta Ads l'est de Facebook : « LinkedIn Ads » au Contexte ouvre l'onglet payant et non l'organique — le payant se reconnaît **avant** l'organique du même nom dans `networksFromContextName`, sinon le compte publicitaire n'avait nulle part où aller. Un réseau payant n'ouvre **aucun couloir de planning** (`isPaidNetworkName`, `setup.ts`) : on n'y publie rien. Aucun connecteur encore : l'onglet dit qu'il attend le sien.

**La démonstration Bondet n'existe plus** (`src/lib/demo/bondet.ts` supprimé) : le Reporting lit exclusivement la base, remplie par le connecteur, et un mois sans données le dit. **La Finance tourne sur du réel.** Aucune API régie n'est branchée ; Airwallex, si — première source réelle du projet, synchronisée toutes les heures depuis GitHub. Les données de démo Bondet sont calées au centime sur le Looker réel de juin 2026 : elles servent de référence visuelle, ne les modifie jamais sans que je le demande. Quand une source réelle arrive, elle ne remplace pas le jeu de démo, elle s'ajoute derrière un flag.

**Pas encore construit**, malgré ce que la section Architecture décrit comme cible :

- La page Finance vit sous `/entreprise/finance`, pas sous `/finance` comme le décrit la section Architecture.
- ~~Le connecteur Meta~~ — **construit et rebranché** le 30/08 avec les quatorze portées (la commande `pnpm sync` fantôme a été retirée de `package.json` ; la synchronisation passe par le cron `/api/cron/sync-reporting` et le bouton). Le relevé de Modération tourne en production ; le premier passage Reporting avec le nouveau jeton reste à constater à l'écran.
- ~~Sélecteur de période, drill-down~~ — construits (plage libre `?du=&au=`, drill-down par ad set `?adset=` sur Meta Ads). Les filtres croisés au-delà (cliquer un segment Persona) restent à faire : les ventilations sont collectées au grain compte, pas ad set.
- ~~Partage public~~ — **construit** (`/partage/[token]`, voir sa ligne). L'export PNG n'existe pas ; le PDF passe par l'impression du navigateur, décision assumée free tier.
- La couche i18n exigée par `PROMPT-V1.md:138` — tous les libellés sont en dur, `LOCALE = "fr-FR"` est une constante littérale. **Décision (30/08) : on n'y touche pas tant qu'aucun client anglophone n'existe** — la demande produit est un outil français.
- ~~`docs/meta-setup.md`~~ — écrit (app, cas d'utilisation, quatorze portées, refus structurels), avec `docs/monitoring.md` (alarmes) et `docs/recus-setup.md`.

**L'application est en accès ouvert, et la RLS ne protège donc plus rien.** La décision est une constante versionnée, `OUVERT_PENDANT_LA_CONSTRUCTION = true` dans `src/lib/access-mode.ts` — pas l'absence d'une variable d'environnement. La passer à `false` referme partout ; `ANTIDOTES_OPEN_ACCESS` permet de trancher par environnement sans toucher au code. L'ancienne variable `ANTIDOTES_REQUIRE_LOGIN` n'est plus lue. Trois conséquences en cascade, toutes délibérées et commentées :

1. `src/lib/supabase/proxy.ts:68` — retour anticipé avant toute redirection vers `/login`.
2. `src/lib/supabase/server.ts:42-49` — `createClient()`, le client de lecture par défaut, renvoie un client **`service_role`**. Les 79 politiques deviennent décoratives.
3. `src/lib/auth.ts:35,83-112` — le visiteur anonyme **emprunte l'identité du premier owner en base**, avec `isOwner: true` sur tous les espaces. Les gardes `require*` et le 404 des modules internes ne s'appliquent donc plus à personne.

Bandeau rouge « Accès public » dans l'en-tête (`src/components/app-header.tsx:50-57`) tant que le mode est actif. Le code d'auth n'a jamais été retiré. À refermer avant le deuxième client : ses données ne t'appartiennent pas.

## Stack

Next.js 16.2.12 · React 19.2.4 · TypeScript strict · Tailwind 4 + shadcn style `base-nova` sur `@base-ui/react` (pas Radix) · Supabase (Postgres, magic link, RLS, Storage) · Recharts · Vercel + Vercel Cron · Vitest 4 + Playwright · Node ≥ 22, pnpm 10.18.2.

**La CI est minimale** — `.github/workflows/ci.yml` lance `typecheck`, `lint`, `test` et `build` à chaque push et sur chaque pull request. Deux workflows l'accompagnent : `db-admin.yml` (manuel — migrations, seed Finance, tests d'isolation contre la vraie base) et `finance-sync.yml` (horaire — appelle `/api/cron/sync-finance`). Elle ne joint aucun service : les suites d'isolation y sautent faute de `SUPABASE_SERVICE_ROLE_KEY`, et **son vert n'est donc pas une preuve d'isolation**. Ni les migrations, ni Playwright, ni le déploiement ne passent par elle.

**Contrainte dure : rester dans les tiers gratuits.** Avant d'ajouter un cron, une dépendance, un service externe ou un appel LLM récurrent, vérifie que ça tient dans le free tier et dis-moi le coût estimé.

Conséquences non négociables :
- Aucun fetch live vers une API tierce depuis le client. Sync par cron → base → lecture locale.
- Toute donnée externe est persistée en base, upsertée par ID externe (idempotent, pas de doublons).
- Aucun secret côté client, aucun secret commité.

### Ce qui plafonne

**Vercel Hobby — 2 crons, une fois par jour maximum.** La cadence est prouvée dans ce repo : une planification plus rapide ne fait pas que se dégrader, elle fait **rejeter le déploiement entier** (commit 9395d5c). Le plafond de deux crons vient de la doc Vercel, pas d'un essai ici. **Les deux créneaux sont engagés** : `/api/cron/mon-travail` à `0 4 * * *` et `/api/cron/sync-reporting` à `0 5 * * *` (le connecteur Meta — Meta accepte les IP de Vercel, contrairement à Airwallex). Plus aucun créneau libre : tout cron supplémentaire se greffe sur le workflow GitHub horaire, comme la publication automatique du Planning (`pnpm publier:planning`, étape d'`airwallex-sync.yml` qui ne travaille qu'à 16h de Paris). Tout ce qui parle à Airwallex tourne ailleurs, et ce n'est pas un choix de quota : **Airwallex refuse les adresses IP de Vercel** (mêmes clés, jeton depuis une machine GitHub, `403 Forbidden` HTML depuis une fonction Vercel — établi par une sonde jouée des deux endroits). `.github/workflows/airwallex-sync.yml` exécute donc `pnpm sync:finance`, `pnpm publier:planning`, `pnpm sync:moderation` (commentaires Meta — Meta accepte les IP GitHub, quelques secondes par compte affecté) puis `pnpm sync:recus` toutes les heures sur un runner GitHub, via la condition `--conditions=react-server` qui neutralise `server-only`. Les Reçus n'ont plus de cron Vercel : la route `/api/cron/recus` reste comme point d'entrée de secours mais ses étapes Airwallex échouent depuis Vercel. Budget minutes GitHub : le schedule tourne en fait ~400 fois par mois — **GitHub en laisse tomber près d'une sur deux** — à une minute l'unité (56 s mesurées le 18/08/2026, installation comprise), sur les 2 000 gratuites d'un dépôt privé ; CI comprise on reste large, et c'est pour ça que Reçus et publication partagent le job de Finance au lieu d'avoir le leur. Les déclenchements depuis l'écran s'ajoutent à ce budget, plafonnés par la fenêtre de fraîcheur de dix minutes — c'est le premier poste à regarder si la consommation Actions monte.

**Supabase Free — 500 Mo de base, 1 Go de Storage, 5 Go d'egress par mois, 50 000 MAU, 2 projets actifs.** Le schéma engagé aujourd'hui, compté sur un rejeu complet des migrations : **69 tables, 55 enums, 137 politiques, 184 index, 13 triggers, 15 fonctions `security definer`, 7 buckets tous privés, 5 050 lignes de SQL** sur 57 fichiers de migration. Le poste qui grossira le premier est la base : `ad_metrics_daily` au grain jour × entité, plus les embeddings pgvector 384d de la FAQ. Point d'attention réel : **un projet gratuit est mis en pause après une semaine sans activité**, et le symptôme est une application qui ne répond plus du tout.

**Hors free tier.** Les appels Anthropic — brouillons de la Modération, lecture des factures des Reçus, tous deux en `claude-opus-5`, `max_tokens: 2000` — sont facturés à l'usage. C'est le seul poste payant du projet aujourd'hui. Sans `ANTHROPIC_API_KEY`, les Reçus retombent sur des règles simples à confiance plafonnée à 0,6, donc sans aucun transfert automatique possible : la dégradation est prévue, pas subie.

## Commandes

| Commande | Effet |
|---|---|
| `pnpm dev` | Serveur de développement, http://localhost:3000 |
| `pnpm build` | Build de production |
| `pnpm start` | Sert le build de production |
| `pnpm typecheck` | Vérification TypeScript (`tsc --noEmit`) |
| `pnpm lint` | ESLint |
| `pnpm test` | Vitest — unitaires colocalisés + isolation RLS |
| `pnpm test:watch` | Idem en continu |
| `pnpm test:e2e` | Playwright (`e2e/`, exclu de Vitest) |
| `pnpm db:migrate` | Applique les migrations manquantes |
| `pnpm db:status` | Liste les migrations en attente sans rien appliquer |
| `pnpm import:monday` | Reprend un planning éditorial depuis Monday (`--list` pour explorer) |
| `pnpm import:followers` | Reprend l'antériorité des abonnés relevée sur Looker (`--dry-run`) |
| `pnpm connect:web` | Rattache une propriété Google Analytics à un espace (`--workspace`, `--property`) |
| `pnpm sync:web` | Collecte Google Analytics à la main (`--depuis` pour le rattrapage) |
| `pnpm seed:planning` | Amorce le Planning Éditorial de Bondet (`--reset`) |
| `pnpm seed:moderation` | Données de démonstration de la Modération (`--reset`) |
| `pnpm sync:moderation` | Relève les commentaires Meta des comptes affectés (étape du workflow horaire) |
| `pnpm purge:moderation-demo` | Efface le client de démonstration de la Modération, le jour où le relevé réel prend le relais |
| `pnpm seed:finance` | Données d'amorçage du module Finance |
| `pnpm factures:connexion` | Ouvre une fenêtre par fournisseur à qui il manque une session de récupération de factures, l'une après l'autre. `<marchand>` pour un seul, `--tout` pour toutes |
| `pnpm factures:passage` | Le passage de récupération des factures. Tourne seul chaque jour sur GitHub Actions ; à la main pour ne pas attendre |
| `pnpm recus:confiance` | Liste les fournisseurs des Reçus et leur automatisme. `<domaine>` l'accorde, `--retirer` le reprend — **la règle porte sur le domaine d'expéditeur, pas sur le marchand** |
| `pnpm seed:mon-travail` | Démo de « Mon travail » : espaces clients fictifs, publications du jour, tâches, cycles (`--reset`) |
| `pnpm sync:linkedin` | Collecte LinkedIn à la main (`--espace`, `--depuis`) — étape de db-admin |
| `pnpm diagnostic:tiktok` | Ce que Composio voit de TikTok avant d'écrire le connecteur : configurations d'authentification, comptes actifs du projet, les deux outils emballés, et jusqu'où porte le passage brut. Lecture seule, étape de db-admin — démarches dans `docs/tiktok-connexion.md` |
| `pnpm diagnostic:linkedin` | Ce que Composio voit de LinkedIn : configurations, comptes, pages administrées et abonnés. `--sonde <organisation>` affiche en plus les statistiques brutes. Lecture seule, étape de db-admin |
| `pnpm diagnostic:schema` | Ce que la base contient vraiment : colonnes, privilèges d'API, colonnes invisibles pour PostgREST. Lecture seule, étape optionnelle de db-admin |
| `pnpm import:faq-moderation` | Pose les 68 entrées des boards Monday « FAQ MODÉRATION » (`--dry-run`), idempotent par (client, titre) |
| `pnpm generate:academy-seed` | Refabrique les seeds des deux formations depuis `scripts/data/academy*/`. **`0058` est appliquée : elle doit se régénérer à l'octet près**, ce que garantit le fait de n'écrire `body` que sur les ressources qui en portent un |

Un chantier est vérifié quand `pnpm typecheck`, `pnpm lint`, `pnpm build` et `pnpm test` passent tous les quatre. La CI les rejoue à chaque push, mais lance-les avant de pousser plutôt que de t'en servir comme d'un correcteur. Le typecheck seul ne prouve rien sur le comportement : les deux défauts du commit 588a864 passaient le typecheck et les tests.

**Dès qu'un chantier touche à l'interface, les quatre commandes ne suffisent plus** : il faut avoir regardé l'écran et rejoué l'audit de contraste. Voir « Vérifier une interface ».

À ne jamais lancer sans mon accord explicite : reset de base, déploiement production. **Les migrations font exception depuis le 18 août** : elles s'appliquent toutes seules au push sur `main`, accord donné une fois pour toutes — voir « Migrations ». Le reste de la règle tient.

## Migrations

**Runner maison, pas la CLI Supabase.** Il n'y a ni `supabase/config.toml`, ni `supabase/functions/`, et `supabase` n'est pas une dépendance. Tout passe par `scripts/migrate.ts`, qui se connecte en direct par URI Postgres.

- **Le préfixe est passé à la date : `AAAAMMJJ_nom.sql`** (`20260830_faq_titres_et_client.sql`), et c'est la forme de toute nouvelle migration. Les `NNNN_` historiques restent — cinq numéros ont fini en double, chaque conversation parallèle qui réservait « le prochain numéro » recréait la collision, et le tri lexicographique du runner fait passer `2026…` après `0065` sans rien casser. Plusieurs migrations le même jour : suffixe lettre (`20260830a_`, `20260830b_`) — l'ordre alphabétique du **nom** ne suffit pas (`rls` < `schema`).
- **Ne renomme jamais une migration appliquée** : `app.schema_migrations` trace par nom, un fichier renommé passe pour jamais joué et se **rejoue**. Les 0056-0058 Academy gardent leur numéro en collision avec 0056-0057 `client_reports` pour cette raison précise — les deux paires sont appliquées, chacune sous son nom.
- Fichiers dans `supabase/migrations/`, **triés lexicographiquement** — le préfixe est structurant, pas décoratif.
- Schéma et RLS sont **deux fichiers distincts** : `0006_planning_schema.sql` puis `0007_planning_rls.sql`. Suis ce découpage.
- Une **transaction par fichier**, tracée dans `app.schema_migrations` (nom, checksum SHA-256, date). Une migration déjà appliquée dont le contenu change affiche `⚠ modifiée depuis` et **n'est pas rejouée** : ne modifie jamais une migration passée, ajoutes-en une.
- Exige `SUPABASE_DB_URL`, qui **ne figure pas dans `.env.example`** et se lit depuis `.env.local`. La chaîne se prend au bouton **Connect** du tableau de bord Supabase (l'ancienne section *Settings → Database* n'existe plus), option **Session pooler** : la connexion directe n'écoute qu'en IPv6, ce qu'un runner GitHub Actions ne sait pas joindre. Pour le rejeu obligatoire sur un Postgres jetable local, ajouter `?sslmode=disable` à l'URI — le runner n'exige le TLS que pour Supabase.
- **Le numéro 0009 n'existe pas** — deux branches parallèles ont réservé leurs numéros. Trou sans conséquence, ne pas chercher à le combler.
- `supabase/seeds/` n'est **jamais lancé** par `db:migrate` : c'est du SQL à coller à la main dans l'éditeur Supabase. Les seeds exécutables sont les scripts TypeScript, idempotents par UUID stable dérivé d'un SHA-256.
- Les migrations de données (`0003_seed`, `0008_planning_bondet`) sont bien appliquées par `db:migrate` et sont idempotentes.
- **Les migrations partent toutes seules au push sur `main`** — job `migrer` de `ci.yml`, ajouté le 18 août. Il ne démarre qu'**après** les quatre commandes vertes (`needs: verifier`), ne tourne ni sur une branche ni sur une pull request, et fait la queue sans s'annuler : couper une migration en cours laisserait le schéma à mi-chemin. Sans `SUPABASE_DB_URL` dans les secrets, il échoue en le disant au lieu de finir vert sans rien faire.
- Cela **ne dispense pas du rejeu sur un Postgres jetable avant de pousser** : c'est maintenant le seul filet. Une migration fautive part désormais toute seule, et le runner ne rejoue jamais ce qui est appliqué — il faudra une migration corrective, jamais une retouche du fichier passé.
- `db-admin` reste, et sert toujours : seeds, diagnostics, imports, tests d'isolation, et le rattrapage d'une base en retard. Il n'est simplement plus obligatoire pour le schéma.

**`src/lib/supabase/database.types.ts` est écrit à la main**, pas généré — 610 lignes qui doivent rester alignées sur 2 919 lignes de SQL, sans commande de génération branchée et sans garde-fou. Toute migration qui touche une table oblige à mettre ce fichier à jour dans le même commit. Son `Enums` ne contient d'ailleurs que les 10 enums de `0001` sur les 42 déclarés : les 32 autres n'y sont jamais entrés.

## Conventions

### Base de données

- **snake_case** partout, tables au pluriel, préfixe par module : `planning_*`, `receipt_*`, `moderation_*` / `faq_*` / `conversations`. Les tables du socle n'ont pas de préfixe (`workspaces`, `memberships`, `ad_metrics_daily`).
- **Enums Postgres**, pas des `text` libres : 40 types déclarés. Un nouveau statut se déclare dans une migration, il ne s'invente pas dans le code.
- **La colonne de tenant est dénormalisée sur chaque table**, y compris les tables filles dont le parent la porte déjà (`planning_subjects` porte `lane_id`, `month_id`, `board_id` **et** `workspace_id`). Redondance assumée : elle permet à la politique RLS de trancher sans jointure.
- **Trois colonnes de tenant coexistent**, une par génération de module. C'est un piège : vérifie laquelle s'applique avant d'écrire une politique.

  | Colonne | Modules | Membership | Helpers RLS |
  |---|---|---|---|
  | `workspace_id` | socle, dashboards, Planning | `organization_members` + `memberships` | `app.accessible_workspace_ids()`, `app.owns_workspace()` |
  | `client_id` | Modération (13 tables) | `moderation_members` | `app.moderation_client_ids()`, `app.moderation_writable_client_ids()`, `app.is_moderation_owner()` — plus `app.faq_readable_client_ids()` (20260830), le seul qui passe par `memberships` : il ouvre la FAQ aux membres de l'espace rattaché, client compris |
  | `org_id` | Reçus (5 tables), Finance (8 tables), Mon travail (3 tables), Academy (6 tables) | `organization_members` | Reçus : `app.receipt_readable_org_ids()`, `app.is_receipt_owner()` — Finance : `app.member_org_ids()`, `app.is_org_owner()` — Mon travail : `app.is_org_owner()` seul, lecture comprise (owner-only) — Academy : `app.academy_course_ids()` et `app.academy_org_ids()` en lecture (membre **ou inscrite**, publié seulement), `app.is_org_owner()` en écriture |

  Les helpers ne sont pas interchangeables. Tous sont `security definer` avec `set search_path = public, pg_temp` — reproduis-le, c'est ce qui empêche un détournement par schéma.
- **Toutes les politiques ciblent `to authenticated`.** Aucune ne vise `anon`, `public` ou `service_role` ; aucune n'utilise `using (true)` ; toute policy `insert`/`update`/`all` porte un `with check`. Ne dévie pas de ces quatre règles.
- Chaque migration RLS termine par un `revoke` nominatif de ses tables pour `anon` — le `revoke all on all tables` de `0002` ne couvre que les tables existant à cet instant.
- **Dates** : `timestamptz` pour un instant — 77 colonnes, **zéro `timestamp` sans fuseau**, `default now()` partout, jamais `timezone('utc', now())`. `date` nu pour un jour métier (12 colonnes, dont 3 membres d'une clé primaire : `ad_metrics_daily.date`, `ad_breakdowns_daily.date`, `social_followers.date` ; `planning_months.month` calé au 1er du mois).
- **Montants, deux régimes.** Comptabilité (Reçus, Finance) en **entiers de centimes** : `amount_cents bigint` + `currency char(3)` à côté, jamais de flottant sur de l'argent. La convention est `cents = montant × 100` **quelle que soit la devise**, y compris celles sans décimales à l'affichage : la division par 100 est un invariant de lecture, pas un calcul. Métriques publicitaires en `numeric(14,4)` et sponsoring du planning en `numeric(12,2)`, tous deux **sans colonne de devise** — l'EUR y est codé en dur à l'affichage (`src/lib/format.ts:15-20`, `lane-table.tsx:74`, `month-group.tsx:91`).
- **Rien n'est jamais converti.** Aucune devise pivot, aucun taux de change nulle part. Les totaux se font **par devise** (`Record<string, number>`), et une dépense qui porte deux montants — facturé chez le commerçant, débité du wallet — les affiche côte à côte sans les additionner (`src/lib/finance/money.ts`). La seule somme inter-devises du repo sert à trier, jamais à afficher, et le dit dans son commentaire. Ne l'imite pas ailleurs.

### TypeScript

- **`type`, pas `interface`** — 223 contre 13. Raison écrite en tête des trois `types.ts` de module : postgrest-js a besoin de l'index signature implicite que TypeScript ne donne qu'aux alias de type pour inférer les résultats de requête. Les `interface` survivantes sont hors chemin Supabase.
- **Zéro `enum` TypeScript.** Les enums Postgres sont mirroirés en **unions de littéraux**, doublées d'un `Record<Union, string>` de libellés français : ajouter une valeur casse la compilation du `*_LABELS`, ce qui est le garde-fou d'exhaustivité.
- **Zéro `any`, zéro `@ts-ignore`, zéro `@ts-expect-error` dans tout le repo.** Tiens ce standard. Le remplaçant est `unknown`.
- La sortie de postgrest se cast en **`as unknown as T[]`** (35+ occurrences), l'entrée en `as never`. C'est le contournement admis, ne cherche pas mieux.
- **`Database` n'est jamais utilisé pour typer une ligne** — uniquement comme générique du client (`createServerClient<Database>`). Le sens est inversé par rapport à l'usage Supabase habituel : chaque table est un alias plat exporté, et `Database` est assemblé à partir d'eux.
- Chaque module a son `types.ts`, dans cet ordre : union littérale → `*_LABELS` → sous-ensembles → prédicat → `type XRow`.
- Les lignes de base gardent le **snake_case de Postgres jusque dans le TS** (`source.email_address`). Pas de couche de mapping. Seuls les **modèles de domaine calculés** sont en camelCase (`RawMetrics.linkClicks`).
- `import "server-only"` en première ligne de tout fichier qui touche la base ou un secret (15 fichiers).
- Les variables d'environnement passent par `src/lib/env.ts` (zod, `parse` au démarrage). Ailleurs, zod ne sert **qu'à la frontière d'entrée** : les 5 Server Actions, en `safeParse`. Jamais dans une query, jamais dans un composant. Zod v4 : `z.uuid()`, pas `z.string().uuid()`.
- **Tout calcul de date se fait en UTC** (`Date.UTC`, `getUTC*`, `timeZone: "UTC"`). Un mois qui commence à minuit heure de Paris décalerait toutes les agrégations. `Europe/Paris` n'apparaît qu'à **un seul endroit** de `src/` (`subject-row.tsx:334`) et dans `playwright.config.ts` — cinq autres formatages de date omettent le fuseau et dépendent donc du serveur : ne recopie pas cet oubli, passe `timeZone` explicitement.
- Formats français centralisés dans `src/lib/format.ts` — jamais de `toLocaleString` en dur dans un composant. Espace insécable fine pour les milliers, virgule décimale, `2 572,22 €`, `0,73 %`. Une métrique non définie s'affiche `—`, un delta sans comparaison `N/A`.
- **Seules les grandeurs additives sont stockées.** Tout ratio (CPA, ROAS, CTR, CPM, CPC, CPL) est recalculé depuis les agrégats bruts de la période affichée. Une moyenne de moyennes est fausse, et c'est le piège classique de ce genre d'outil.

### Système visuel

**Toutes les valeurs vivent dans `src/styles/tokens.css`, et nulle part ailleurs.** `globals.css` ne fait que les brancher sur Tailwind et sur le vocabulaire shadcn (`--card`, `--muted`, `--primary`…) : repointer ces alias suffit à faire basculer l'application entière sans rouvrir un composant. Aucun hex dans un `.tsx` — sauf les couleurs de statut du board Monday, qui sont des **données** du client. Celles-là ne se retouchent pas : c'est l'encre du libellé qui s'adapte, choisie d'après la luminance du fond (`chipInk`, `planning/cells.tsx`). Du blanc sur « EN BROUILLON » tombait à 1,79:1.

- **Canvas `--canvas`, surfaces `--surface`.** Les cartes ressortent en blanc sur un gris chaud, bordées et posées sur `shadow-card`. L'inverse — cartes grises sur fond blanc — les enfonçait.
- **Couleur vive ≠ couleur de texte.** Le vert de marque est à 2,71:1 sur blanc : illisible en texte, et le blanc posé dessus l'est autant. Chaque famille a donc deux jetons — la teinte vive pour les fonds, les points et les marques, une **encre** (`--accent-ink`, `--danger-ink`, `--warning-ink`, `--info-ink`) pour tout ce qui se lit. Le bouton primaire est l'encre, jamais le vert. `--text-tertiary` (2,79:1) est réservé aux icônes.
- **Jamais de blanc posé sur `--accent-ink`.** L'encre d'accent s'inverse en sombre (`#2f5320` → `#b7e99b`, un vert clair) : un glyphe ou un texte blanc dessus tombe à 1,39:1 et disparaît — payé trois fois (sélecteur de conversions, stylo de génération, flèche de tri). Tout aplat qui porte du blanc ou une icône claire utilise le couple `bg-primary` / `text-primary-foreground`, qui s'inverse ensemble ; `--accent-ink` reste une couleur de **texte** posé sur une surface, jamais un fond sous du blanc.
- **L'échelle typographique est préfixée `type-`**, pas `text-` : `type-h1`, `type-body`, `type-overline`, `type-stat`… Ce n'est pas cosmétique — tailwind-merge range tout `text-*` dans le même groupe que les couleurs et **en supprime une des deux** au passage dans `cn()`. Trois libellés étaient muets avant qu'un audit du rendu ne le montre. Ne recrée jamais une classe utilitaire préfixée `text-`.
- **Une carte a trois formes et pas une de plus** (`src/components/ds/surface.tsx`) : `Panel` (contenant), `NavCard` (cliquable, doit porter des métriques), `StatCard` (un chiffre). Seule `NavCard` réagit au survol — un effet sur une carte non cliquable ment sur ce qui va se passer.
- **Aucune valeur inventée.** Une source absente affiche `—` et le dit ; l'absence de ligne pour un espace vaut zéro, pas « inconnu » (`NO_WORKSPACE_ACTIVITY`).
- Le mode sombre est **maintenu** : chaque token est redéclaré sous `.dark`.
- Contraste : le seuil de 4,5:1 se vérifie **dans le navigateur**, jamais sur le papier. Attention, Tailwind émet les opacités en `oklab(...)` — un audit qui parse les couleurs à la main produit de faux positifs ; faire résoudre les couleurs par un canvas.

Le reste du vocabulaire, à ne pas réinventer :

- **Rayons** `--r-sm` 8 / `--r-md` 12 / `--r-lg` 16 / `--r-xl` 20 / `--r-pill`. Une carte est en `rounded-lg`, un bouton et un champ en `rounded-md`, une pastille en `rounded-pill`.
- **Espacements** : l'échelle Tailwind native suffit (`gap-5` = 20 px entre cartes, `p-5` dans un panneau, `space-y-5` entre blocs d'une page). Aucune valeur arbitraire entre crochets.
- **Mouvement** : `duration-(--motion-duration) ease-standard` — 150 ms, une seule courbe. Les transitions portent sur `background`, `border-color`, `box-shadow`, `transform`, `opacity`, jamais sur `all`.
- **Icônes Lucide, `strokeWidth={1.75}`**, `size-4` dans le texte, `size-4.5` dans une navigation, `size-5` dans un état vide. Une icône est décorative : `aria-hidden`, et l'information est dans le texte à côté.
- **Boutons** : hauteur 40 px par défaut (`size="sm"` = 32 px pour une action secondaire dans un en-tête). Le primaire est l'encre `#1A1A1A`. La variante `accent` — vert foncé, texte blanc — est **réservée à la création** (« Ajouter »). Un seul aplat d'accent par écran.
- **Champs** : hauteur 40 px, bordure `--border-line`, anneau de focus `2px` en `--accent` à 20 %. Le focus clavier est visible partout, sans exception.

### Composer une page

Toute page applicative suit la même charpente. Ce n'est pas un goût, c'est ce qui fait qu'un écran nouveau se lit sans mode d'emploi.

1. **`<AppShell viewer title subtitle>`**, jamais autre chose. Il fournit le rail, la barre de page collante, le conteneur 1440 px **et le `<main>`** — une page qui ouvre le sien produit un `main` imbriqué et une double marge. Le titre est passé par le layout qui connaît la section ; la page n'affiche pas de `h1` à elle, sinon le nom apparaît deux fois.
2. **Une bande de mesures** en tête, quand le module a des chiffres : `<StatGrid>` et quatre `<StatCard>` au plus, `grid-cols-2 lg:grid-cols-4`. Elles répondent aux quatre questions qu'on se pose en ouvrant la page. Chaque carte porte un libellé en `type-overline`, un chiffre, et **une phrase de contexte** — sans elle, « 12 » ne renseigne sur rien. Les indicateurs ne se cachent pas à l'intérieur des blocs.
3. **Le contenu en `Panel`**, un par sujet, avec `PanelHeader` (titre, compteur, description, action à droite) puis `PanelBody` ou `PanelRows` pour une liste qui doit toucher les bords. Les titres de section entre panneaux sont des `SectionHeader`, pas des libellés gris pâle en capitales.
4. **Les cartes en grille** : `grid-cols-1 md:grid-cols-2 xl:grid-cols-3`, `gap-5`. Jamais une carte seule sur toute la largeur.
5. **L'archivé, l'accessoire et le rare descendent en bas de page.**

Les règles qui se sont payées cher :

- **Une liste longue, c'est un panneau, pas N cartes.** Douze mois de planning en douze cartes flottantes donnaient le même poids visuel aux onze mois vides qu'au seul qu'on vient regarder. Un `Panel` + un rang par élément, l'élément courant ouvert et surligné.
- **Deux niveaux de navigation ne se ressemblent pas.** Les onglets de section sont des pastilles à encre pleine dans une gouttière creuse (`DashboardNav`) ; les onglets d'un cran plus bas sont **soulignés** (`BoardTabs`). Deux rangées de pastilles identiques mentent sur la hiérarchie.
- **Un état d'entité est une `StatusPill`**, jamais un badge maison. Le ton est sémantique : `positive` favorable, `warning` ce qui attend une action de ma part, `danger` retard ou échec, `info` planifié, `neutral` archivé ou sans qualité. Le point reprend l'encre, pas la teinte vive — à 6 px, l'ambre vif tombe à 2:1.
- **Un état vide est court et donne une sortie.** Hauteur réduite, une phrase, un bouton. Mieux encore : quand des données voisines existent, les montrer à la place — « rien à publier aujourd'hui » devient les trois prochaines publications datées.
- **Les filtres vivent dans l'URL, en français** (`?statut=`, `?periode=`). Un filtre se partage par copie du lien et survit au retour arrière. Les groupes de filtres sont des `FilterPills`.
- **Mobile-first pour de vrai.** Une ligne dense se replie en trois niveaux — repère, sujet, puis date et statut. Ce qui ne sert pas au téléphone sort (`hidden md:block`) : sur « À publier », la caption et la vignette mangeaient une ligne entière pour afficher un tiret.
- **Une préférence d'affichage se mémorise dans un cookie, pas dans le stockage local** : c'est le serveur qui rend la première image, c'est donc lui qui doit la connaître. Voir `src/lib/ui-preferences.ts`.
- **Aucun `<main>`, aucun `<h1>`, aucune marge de page dans un composant.** Ils appartiennent au cadre.

### Vérifier une interface

`typecheck`, `lint`, `test` et `build` ne prouvent **rien** sur une interface : les défauts les plus coûteux de cette session passaient les quatre. Une refonte n'est vérifiée que regardée.

- **Sur `pnpm build && pnpm start`, jamais sur `pnpm dev` seul.** Dans un bac à sable sans WebSocket, le HMR échoue et React ne s'hydrate pas : tout composant client paraît mort.
- **Prendre une capture et la regarder.** C'est ce qui a montré un bouton dont le texte avait disparu, un rail replié aux libellés tronqués, une ligne mobile cassée.
- **Rejouer l'audit de contraste au navigateur** sur chaque page touchée : parcourir les nœuds de texte, composer la pile de fonds avec leur alpha, comparer au seuil (4,5:1, ou 3:1 au-delà de 24 px). Faire résoudre chaque couleur par les **pixels** d'un `canvas` (`fillRect` puis `getImageData`) : la sérialisation de `fillStyle` rend l'`oklab(...)` de Tailwind tel quel, seule la lecture de pixels normalise vraiment. Et exempter les contrôles `disabled` — leur opacité à 50 % échoue au seuil par construction, WCAG les exclut. **Second angle mort, celui-là muet : un fond peint par un élément frère.** L'aplat de la pastille active est un `PillIndicator` positionné en absolu à côté du lien, pas un fond sur le lien ; un audit qui remonte la chaîne des ancêtres ne le voit pas et rend « Reporting » à 1,05:1 alors que l'écran montre du blanc sur noir parfaitement lisible. Devant un rapport proche de 1, regarder la capture avant de corriger quoi que ce soit — c'est le script qui a tort.
- **Un fond peint par un frère échappe à cet audit**, qui ne compose que les ancêtres. L'onglet actif d'un espace en est le cas : depuis `PillIndicator`, l'aplat sombre est un élément **positionné par-dessus**, pas le fond du lien — l'audit voit du blanc sur blanc et crie à 1,05:1 sur un onglet parfaitement lisible. Vérifier à la capture avant de « corriger » : le même piège attend tout indicateur glissant.
- **Faire tourner l'application sans Supabase, si besoin** : un binaire PostgREST devant le Postgres jetable, plus un petit proxy qui traduit `/rest/v1/*`, répond 401 sur `/auth/v1` (l'accès ouvert bascule alors sur l'owner) et `[]` sur `/storage/v1`. Penser à relayer l'en-tête `Content-Range`, sans quoi toute pagination affiche « 0 élément » alors que les lignes sont là. Et se souvenir que **les `NEXT_PUBLIC_*` sont figées à la compilation** : changer de cible impose de rebuilder, sinon l'application interroge l'ancienne adresse et rend des 404 inexplicables. Deux détails qui ont coûté chacun une demi-heure : des fixtures à UUID fabriqués doivent porter le **nibble de version 4** (`z.uuid()` de Zod 4 refuse un UUID de version 0, et les routes rendent alors « Requête invalide » sans que rien d'autre ne cloche) ; et dans un bac à sable qui fauche les démons entre deux commandes, Postgres, PostgREST et le proxy se lancent en tâches de fond suivies par le harnais, pas en `nohup &`.

### Nommage et langue

- **Fichiers en kebab-case, 148/148.** Aucune majuscule dans un nom de fichier. Dossiers en minuscules, segments dynamiques `[workspace]`, `[client]`, `[board]`.
- Composants React en PascalCase, `export function`, **jamais `export default`** — sauf `page.tsx` et `layout.tsx`, où Next l'impose.
- Préfixes de fonctions serveur : `get*`/`list*` en lecture, `require*` pour une garde, verbe + nom pour une mutation, `evaluate*`/`should*`/`is*` pour une décision pure. Constantes exportées en `UPPER_SNAKE_CASE`.
- Tests : **`*.test.ts` colocalisés à côté du code** (20/20). `*.spec.ts` est réservé à Playwright dans `e2e/`. Les tests qui attaquent une vraie base vivent dans `tests/`.
- **Le code est en anglais, tout ce que lit un humain est en français.** Identifiants, tables, colonnes, valeurs d'enum : anglais. Commentaires, textes UI, messages d'erreur, noms de `describe`/`it`, **segments d'URL et query params** : français (`/entreprise/recus`, `?statut=`, `?nonlus=1`, `?suivant=`). Le pont entre les deux est le `*_LABELS` de chaque `types.ts`.
- Guillemets doubles partout. Les 14 fichiers de `src/components/ui/` sont sans point-virgules : ils sont générés par shadcn, laisse-les tels quels.
- Les commentaires expliquent **pourquoi**, pas quoi.
- **Sobriété des textes d'interface — demande explicite d'Alessandro (28/08/2026).** Pas de phrase d'explication sous un titre, pas de mécanique interne exposée à l'écran (cadence de synchronisation, provenance d'un relevé, ce que fera un bouton « on s'en doute »). Un libellé, un chiffre, point. Le pourquoi vit dans les commentaires du code, jamais dans l'UI. Un en-tête de menu se distingue typographiquement des entrées cliquables (overline, pas la même typo que les items).

### Clients Supabase

Quatre fichiers dans `src/lib/supabase/`, trois constructeurs. Choisis en connaissance de cause :

- `client.ts` → navigateur, clé anon, RLS active. **Un seul call site légitime**, le formulaire de login. Aucune donnée métier ne se fetche côté client.
- `server.ts` / `createClient()` → le défaut pour toute lecture serveur. Clé anon et RLS **sauf en accès ouvert**, où il bascule en `service_role`.
- `server.ts` / `createAdminClient()` → `service_role`, contourne la RLS. Réservé à trois usages écrits dans le fichier : cron, callback OAuth, écritures d'audit. Jamais pour répondre à une requête utilisateur.
- `proxy.ts` → rafraîchit la session. `getUser()` et non `getSession()` : seul le premier revalide le jeton.

Il n'y a **pas de `middleware.ts`** : Next 16 l'a renommé, c'est `src/proxy.ts` et il exporte `proxy()`.

### Structure d'une route API

Modèle : `src/app/api/recus/connexion/route.ts`.

- `export const dynamic = "force-dynamic"`. Pas de `runtime` déclaré nulle part — le Node par défaut est implicite.
- Le contrôle d'accès vient **en premier**, avant tout travail.
- Module interne ⇒ `404`, jamais `403`.
- Un aller-retour OAuth pose un état anti-rejeu dans un cookie `httpOnly`, vérifié au retour en `timingSafeEqual`.

### Structure d'un cron

Modèle : `src/app/api/cron/recus/route.ts`.

- `GET`, `dynamic = "force-dynamic"`, `maxDuration` déclarée.
- **Le cron s'authentifie lui-même** : en-tête `Bearer` comparé à `CRON_SECRET` avec `timingSafeEqual` — un `===` fuiterait, par sa durée, combien de caractères de tête sont corrects. Il n'y a pas de vérification d'en-tête `x-vercel-*`.
- Le chemin `/api/cron` est déclaré dans `PUBLIC_PATHS` de `src/lib/supabase/proxy.ts`. Ce n'est pas une exception à l'authentification, c'en est une autre forme.
- `createAdminClient()` : un ordonnanceur n'a pas de session, donc pas de RLS.
- **Toujours tester le `error` de Supabase, pas seulement le `data`.** Une table absente rend un `data` nul, donc une liste vide, donc un « rien à faire » parfaitement rassurant — et un cron muet pendant des semaines. C'est l'inverse de la règle des `queries.ts`, où l'erreur est volontairement ignorée parce que la RLS est l'autorité et qu'une liste vide est la bonne réponse.
- **Répondre 200 même en échec partiel**, erreurs dans le corps. Un 500 ferait rejouer par l'ordonnanceur ce qui a déjà réussi.
- L'ordre des étapes est une décision produit, pas un détail d'implémentation : les dépenses avant les mails, la vérification en dernier.

### Lecture et écriture

- **Lectures** : un `lib/<module>/queries.ts`, `import "server-only"` en ligne 1, un objet d'options nommé (jamais de paramètres positionnels), `Promise<T[]>` explicite, `limit(options.limit ?? 100)`, `const { data } = await query` sans gérer l'erreur.
- **Écritures** : uniquement des Server Actions dans `src/app/actions/`, un fichier par module, jamais d'action inline. Signature `(_previous: XResult | null, formData: FormData)` imposée par `useActionState`. `safeParse` en entrée, gardes qui `throw`, `try/catch` qui convertit en union discriminée `{ ok: true, message } | { ok: false, error }`, `revalidatePath`, puis toast `sonner` côté client.

### Les fichiers à lire avant d'écrire

| Pour écrire… | Lire d'abord | Ce qu'il faut en copier |
|---|---|---|
| un cron | `src/app/api/cron/recus/route.ts` (111 l.) | Garde `Bearer` + `timingSafeEqual`, `try/catch` par unité de travail accumulé dans `errors[]`, réponse 200 `{ ok, report, errors }` |
| de la logique métier | `src/lib/recus/auto-forward.ts` (177 l.) + `src/lib/recus/types.ts` | Fonction **pure**, zéro import Supabase, état passé par un `type XContext`, retour en union discriminée, union de refus doublée d'un `REFUSAL_LABELS` |
| une lecture en base | `src/lib/moderation/queries.ts` (177 l.) | `server-only`, `export type XFilters`, `let query` enrichie par `if (filters.x)`, cast `as unknown as T[]` |
| une page (données) | `src/app/moderation/[client]/page.tsx` (70 l.) | `type Params = Promise<…>` awaité, `generateMetadata` séparée, garde `require*` qui `notFound()`, filtres lus en query params français, `Promise.all`, tout passé en props à un seul composant client |
| une page (interface) | `src/app/entreprise/finance/page.tsx` | `AppShell` → `SectionHeader` → `StatGrid` de quatre `StatCard` → `Panel` par sujet. Chiffres réels ou `—`, jamais de valeur inventée |
| un composant du système | `src/components/ds/surface.tsx` (165 l.) | Trois formes de carte et pas une de plus, `BASE` partagé, survol réservé au cliquable, en-tête à titre + compteur + action |
| un test | `src/lib/recus/matching.test.ts` (188 l.) | Colocalisation, import relatif du sujet, fabriques de fixtures en arrow avec `Partial<T>`, un `describe` par fonction exportée nommé du nom exact de la fonction, `it()` en français, zéro mock |

Pour un test d'isolation RLS, le modèle reste `tests/planning-isolation.test.ts`. Avant d'écrire un nouveau module, lis le module existant le plus proche et suis son pattern plutôt que d'en inventer un.

## Skills

Le dépôt embarque des skills dans `.claude/skills/`, certains en lien symbolique vers `.agents/skills/`. Rien ne les déclenche mécaniquement : ce qui suit est une consigne, pas un automatisme du harnais.

**À charger d'office, sans qu'on te le demande :**

| Skill | Avant de… |
|---|---|
| `supabase-postgres-best-practices` | écrire une migration, une politique RLS, un index, un trigger ou une fonction `security definer`. Ses pièges — `security definer` qui contourne la RLS, `update` sans `with check`, vue qui l'ignore par défaut — recoupent les conventions ci-dessus, et aucun ne se voit au typecheck ni aux tests. |
| `supabase` | toucher à l'auth, aux clients Supabase, au Storage ou aux sessions. **Ignore sa section « Making and Committing Schema Changes »** : elle décrit la CLI Supabase, qui n'est pas utilisée ici. Les migrations passent par `scripts/migrate.ts` et des fichiers `NNNN_nom.sql` numérotés à la main. |
| `dataviz` | écrire un graphe Recharts, une carte KPI, une jauge ou un écran de pilotage. À croiser avec le design system existant de `src/components/viz/`, qui fait foi en cas de contradiction. |
| `vercel-react-best-practices` | écrire un composant serveur ou client non trivial, ou changer une frontière de rendu. |

**À n'activer que si je le demande :**

- `improve` — audit et plans d'implémentation. Utile en revue de chantier, trop coûteux en réflexe.
- `caveman` — réponses compressées. Jamais en automatique : quand tu m'expliques un arbitrage, la clarté passe avant les tokens.
- `agent-browser` — pilotage de navigateur, pour vérifier une preview.
- `ui-ux-pro-max`, `ui-styling`, `banner-design`, `brand`, `design`, `design-system`, `slides` — production graphique. Les scripts de génération d'images exigent `GEMINI_API_KEY`, qui n'est pas fournie : ils échouent proprement sans elle.
- `azure-aigateway` — sans rapport avec la stack, installé par curiosité.

## Git et livraison

- `main` est la branche stable. Le travail se fait sur des branches / worktrees dédiés.
- **Commite et pousse au fil de l'eau sur la branche dédiée, sans me demander.** Tant que rien n'est mergé, `main` reste intact — et l'environnement de travail est éphémère : ce qui n'est pas poussé est perdu avec la session.
- **Tu ne merges jamais dans `main` toi-même.** Quand un chantier est terminé : push, puis donne-moi l'URL du déploiement Vercel de preview. Je valide visuellement, puis je te dis de merger.
- **C'est moi qui déclare une phase terminée**, pas toi. Tu peux dire qu'un chantier te semble prêt ; tu ne le clôtures pas.
- Un chantier n'est « terminé » que si le build passe, les types passent, les tests passent, et la preview est en ligne.
- Commits en français, descriptifs, une intention par commit.
- **Vérifie l'écart avec `main` en début de session.** Ce fichier n'est chargé automatiquement que depuis `main` ; une session qui clone le repo y arrive par défaut et ne voit ni les branches en cours ni ce qu'elles contiennent. Si `main` est loin derrière, dis-le-moi avant de commencer.

## Pièges connus

**Un cron non conforme fait rejeter le déploiement entier.** Vercel valide `vercel.json` à la lecture. Une cadence interdite par le plan ne dégrade pas le comportement : elle annule le déploiement — et le symptôme trompe, puisque rien n'apparaît dans la liste des déploiements au lieu d'une ligne rouge. Deux commits sont ainsi restés en ligne sans jamais être déployés.

**Un cron GitHub n'est pas horaire, et son absence ne se signale nulle part.** Un `schedule` est mis en file au mieux : sous charge, GitHub saute purement la fenêtre. Sur ce dépôt, près d'une exécution horaire sur deux n'a jamais lieu — sans ligne rouge, sans notification, sans trace autre qu'un trou dans la liste des exécutions. Ne jamais écrire à l'écran une promesse de cadence adossée à un `cron` GitHub : afficher l'âge réel de la donnée, et prévoir un déclencheur qui ne dépende pas du planificateur. C'est ce qui a coûté sa fenêtre unique à la publication du Planning, calée sur 16 h de Paris pile : une exécution sautée à 14 h 17 UTC emportait la journée entière. Corrigé — la fenêtre court de 16 h à minuit (`isPublishWindow`), le verrou d'unicité tenant le doublon. **Depuis le 30/08, la panne silencieuse a deux réponses** : `/entreprise/syncs` (owner-only) affiche l'âge réel de chaque source avec un seuil « En retard » à 2 h, et `airwallex-sync.yml` termine par un témoin de vie — succès → ping `HEARTBEAT_URL`, échec → `HEARTBEAT_URL/fail`, silencieux sans le secret. Le montage complet (healthchecks.io + relance horaire cron-job.org par `workflow_dispatch`) est dans `docs/monitoring.md` : **deux gestes restent à faire côté compte** — créer le check et poser le secret, créer la tâche cron-job.org.

**Le proxy d'authentification intercepte les routes cron.** Une route cron parfaitement écrite est renvoyée vers `/login` avant de s'exécuter si son chemin n'est pas dans `PUBLIC_PATHS`. En production, la synchronisation ne démarre jamais, sans que rien ne le signale.

**`create table if not exists` transforme un conflit de schéma en panne silencieuse.** Deux branches parallèles ont décrit `wording_history` : celle des cartes client, appliquée la première à la vraie base, avec `hook`, `org_id`, `full_wording`, `platform`, `published_at` ; celle du Contexte, avec `accroche`. Le `if not exists` — mis là exprès pour la sécurité de fusion — a fait passer la seconde sans rien dire, et le code d'ici a écrit `accroche` dans une table qui attend `hook`. **Trois runs d'isolation ont échoué avant que la cause soit vue**, parce que le message accusait le cache. La table vivante fait foi, 0037 réconcilie une base née des seules migrations d'ici. **À la fusion, le même défaut a changé de visage** : sur une base neuve, `0032_client_context_schema.sql` trie avant `0032_production_schema.sql`, créait la table, et la migration Production échouait sur « relation already exists » — plus aucune base ne pouvait s'amorcer. Le bloc a donc été retiré de 0032, qui affiche désormais « ⚠ modifiée depuis » sans être rejoué : la vraie base garde la table de la Production, et un rejeu à neuf produit la même. Avant d'écrire un `if not exists` sur une table qu'une autre branche déclare aussi, vérifier la forme réellement en base : `pnpm diagnostic:schema <table>`.

**PostgREST sert l'API REST depuis un cache de schéma, et ce cache survit à la migration.** Une colonne fraîchement créée n'existe pas pour lui tant qu'il n'a pas rechargé : il rend `PGRST204 … in the schema cache` sur une base pourtant à jour, et le message accuse une colonne qui existe. C'est ce qui a fait échouer une suite d'isolation lancée juste après des migrations, sur une colonne posée la veille — un faux négatif parfaitement crédible. `scripts/migrate.ts` envoie donc `notify pgrst, 'reload schema'` dès qu'au moins une migration est appliquée. Sur un Postgres nu sans PostgREST, personne n'écoute et l'ordre ne coûte rien.

**Un `error` Supabase non testé devient un silence.** Voir la structure d'un cron ci-dessus. C'est la même erreur qui a failli passer en production dans les Reçus.

**Le SQL ne se relit pas, il se rejoue.** Trois défauts sont sortis d'un rejeu sur un vrai Postgres 16 jetable, qu'aucune relecture n'aurait attrapés : les littéraux d'un `insert ... select` ne sont pas convertis vers un enum comme ceux d'un `insert ... values` (cast explicite obligatoire) ; les politiques de `storage.objects` survivent au `drop table` du module, elles appartiennent au stockage ; et les CTE d'un même ordre partagent un instantané de la base — un seed en une seule requête n'insérait rien au premier passage, la seconde partie ne voyant pas ce que la première venait de créer. **Toute migration se rejoue sur un Postgres jetable avant d'être poussée.** Le bootstrap du jetable doit imiter les *default privileges* de Supabase (`alter default privileges in schema public grant all ... to anon, authenticated, service_role`) : sans eux, toute table créée après 0002 rend des `permission denied` trompeurs en simulation RLS — c'est précisément parce que Supabase accorde tout par défaut que chaque migration RLS termine par son `revoke ... from anon` nominatif.

**L'éditeur SQL de Supabase n'honore pas toujours le `begin`/`commit` d'un fichier collé.** Un script qui échoue à mi-chemin laisse donc des tables à demi créées, et le passage suivant bute sur `create table if not exists` — qui ne fait rien quand la table existe, même sous une autre forme : l'index qui suit cherche une colonne absente et tout s'arrête (`42703`). Un fichier de rattrapage doit **converger**, pas seulement être idempotent : détecter la forme périmée, la remettre à plat si elle est vide, et s'interrompre en le disant si elle porte des données. Et finir par `notify pgrst, 'reload schema'` — sans quoi PostgREST continue de répondre « relation does not exist » sur des tables pourtant créées.

**Un préambule de migration qui supprime sans condition est une bombe.** Passe encore sur des tables vides, désastreux sur un planning rempli. Toute suppression est gardée par la présence d'un vestige de l'ancien schéma, et de lui seul.

**Un `unique` sur une colonne nullable ne contraint rien.** `planning_lanes` et `planning_subjects` portent `unique (board_id, external_id)`, mais `external_id` est nullable : deux lignes créées hors import ne sont pas dédupliquées, `NULL` n'égalant pas `NULL`.

**Un `Menu.GroupLabel` de Base UI hors de son groupe vide le menu.** `DropdownMenuLabel` est un `Menu.GroupLabel` : posé directement dans le `DropdownMenuContent`, il lève « MenuGroupContext is missing » — et le menu **ne rend plus rien du tout**, items compris. Rien ne casse à la compilation, rien n'apparaît à l'écran, et l'erreur ne se lit que dans la console du navigateur. Le titre se met **dans** le `DropdownMenuRadioGroup` (ou `Group`).

**Une constante exportée d'un fichier `"use client"` n'est plus une constante côté serveur.** Importée par un composant serveur, elle devient une référence opaque : la comparaison échoue en silence, rien ne casse, le réglage ne se souvient simplement de rien. Le nom du cookie du rail vit donc dans `src/lib/ui-preferences.ts`, un module sans directive, lisible des deux côtés.

**Le mode développement ne s'hydrate pas dans un bac à sable sans WebSocket.** Le HMR de Next échoue, React ne s'attache à rien, et tout composant client paraît mort — un clic sans effet, un `aria-pressed` figé. Ce n'est pas le code : vérifier tout comportement interactif sur `pnpm build && pnpm start`, jamais sur `pnpm dev` seul.

**Les scripts Node ne lisent pas `.env.local` tout seuls.** Next le fait nativement, `dotenv/config` ne lit que `.env`. Un script lancé à la main doit pointer explicitement sur `.env.local`.

**Un instantané daté du jour où on le lit ment d'un mois par an.** Le compte d'abonnés lu à 5 h le 1er est celui du 31 : le dater du 1er le range dans le mois suivant, et chaque courbe prend un mois d'avance sans qu'aucun test ne le voie — les chiffres sont justes, seule l'étiquette est fausse. Dater un relevé du jour qu'il **clôture**, dès l'écriture. Vu sur tous les clients le 2 septembre 2026.

**Un cron quotidien n'est pas un outil de travail.** « Mon travail » relit Fathom à 4 h du matin — une réunion de 10 h voit donc ses tâches arriver le lendemain, ce qui est le bon comportement pour du fond de tâche et le mauvais pour le geste qu'on a en sortant d'un appel. La route acceptait déjà le propriétaire depuis son navigateur, mais une URL à retenir qui rend du JSON brut n'est pas un bouton : **Relire mes réunions**, dans la barre de page, relance le passage et **dit ce qui s'est passé**. Le message distingue les trois cas que « 0 tâche » confondait — des tâches créées, des réunions relues sans rien de neuf, et une étape qui **ne s'est pas exécutée** faute de `FATHOM_API_KEY`. C'est ce troisième cas qui fait chercher un défaut dans le parseur alors que la clé manque simplement à l'environnement.

**Les tests d'isolation se sautent en silence.** Sans `.env.local` renseigné, la suite passe en `describe.skip` : `pnpm test` est vert **sans avoir rien prouvé** sur la RLS. Un vert n'est une preuve d'isolation que si les tests ont réellement tourné contre la base, migrations appliquées.

**En accès ouvert, la RLS ne protège plus rien.** Voir la section État actuel. L'accès ouvert désarme les 92 politiques d'un coup : c'est un réglage de construction, il n'a rien à faire sur un environnement qui porte les données d'un client.

**`e2e/acces.spec.ts` suppose l'application fermée.** Ses trois cas vérifient la redirection vers `/login` : ils échouent tant que l'accès ouvert est actif. La suite e2e n'est pas dans la CI — elle demande un navigateur et un vrai projet Supabase — donc rien ne le signale. **Décision (30/08) : l'accès reste ouvert, c'est un choix de construction assumé** — la suite reprendra son sens à la fermeture, elle ne se réécrit pas.

**Le proxy tronque les corps de requête à 10 Mo — et un fichier ne doit jamais transiter par une action serveur.** Deux étages de plafonds sur le même chemin : `bodySizeLimit` des actions, et surtout `experimental.proxyClientMaxBodySize` (10 Mo par défaut dès qu'un `src/proxy.ts` existe) qui **coupe** le corps sans erreur claire — l'action reçoit un formulaire tronqué, « Unexpected end of form », 500, et l'exception traversait jusqu'à l'écran d'erreur du navigateur. Les images passaient, une vidéo de 14,9 Mo non. Depuis : les visuels partent du **navigateur directement dans le bucket** (`prepareVisualUploads` signe les URL, PUT côté client, `attachVisuals` accroche — `src/lib/planning/upload-client.ts`), les deux plafonds sont levés à 100 Mo en ceinture, `visualUploadError` refuse avant l'envoi ce qui dépasse 50 Mo par fichier, et `useCellAction` attrape tout échec d'action en toast, jamais en écran noir.

**`@huggingface/transformers` ne charge pas sur Vercel.** Le paquet s'appuie sur `onnxruntime-node`, dont la bibliothèque native (`libonnxruntime.so.1`) n'est pas tracée dans le bundle de la fonction : tout chemin serveur qui importe le module — même dynamiquement — meurt en « Failed to load external module ». Vécu au premier envoi réel d'une correction de Modération : l'embedding de l'entrée FAQ se calculait dans l'action serveur, et son crash emportait aussi l'envoi de la réponse au client. La règle : **le modèle d'embeddings local ne tourne que sur une machine complète** (runner GitHub, poste local), jamais dans une action ou une route Vercel. Ce qui s'écrit côté Vercel pose `embedding_source = null`, et `reindexFaqSearch` rattrape au relevé horaire.

**Un outil pré-emballé n'est pas l'API.** LinkedIn a d'abord semblé incapable de rendre autre chose qu'un compteur cumulé : ses trois formes d'intervalle documentées étaient refusées, et aucun outil n'exposait les publications. C'était faux — c'était l'emballage de Composio qui bridait. `tools.proxyExecute` fait une requête HTTP brute authentifiée par la connexion, et l'API sert alors tout : le grain jour, les 627 publications d'une page, leurs statistiques une à une, les gains d'abonnés mensuels. Une demi-journée de travail avait été bâtie sur la mauvaise conclusion (une table de cumuls, une différence à la lecture, « pas d'antériorité » assumée), et tout est parti à la poubelle. **Devant une API bridée, chercher le passage brut avant d'accepter la limite** — et se méfier d'une limite qui contredit ce que tout le monde fait par ailleurs.

**La forme d'une réponse ne se relit pas, elle se sonde.** Quatre causes se sont empilées avant que LinkedIn ne rende une seule page, et aucune n'était visible du code. (1) Composio range les comptes dans **deux tiroirs** : brancher depuis « All Apps » alimente le tiroir personnel « For You », invisible du projet Platform que l'application interroge — trois connexions existaient, le projet en voyait zéro. (2) La configuration d'authentification par défaut ne demande **pas** les portées d'organisation, d'où un 403 `r_organization_admin` sur toute lecture de page. (3) Composio **refuse `latest`** à l'exécution manuelle d'un outil, avec un message qui accuse la version (« Toolkit version not specified ») alors que rien n'est en cause — `dangerouslySkipVersionCheck` ou une version datée. (4) La passerelle **ne rend pas les ACL** documentées mais la **fiche de l'organisation déjà résolue**, une seule par appel quel que soit `count` : le parseur lisait comme vide une réponse pleine, puis n'en voyait qu'une sur six. Un diagnostic qui affiche la **réponse brute** quand il ne trouve rien (`pnpm diagnostic:linkedin`) a tranché les quatre en trois passages, là où la relecture n'en aurait tranché aucune. À reproduire pour tout nouveau connecteur : sonder avant d'écrire.

**TLS des scripts Postgres, à moitié tranché :** `scripts/migrate.ts` accepte désormais `SUPABASE_DB_SSL_CA` (chemin vers le certificat CA du projet, téléchargeable du dashboard) et vérifie alors la chaîne ; sans la variable, il retombe sur `rejectUnauthorized: false` — chiffré mais non authentifié — et la branche `sslmode=disable` du Postgres jetable est intacte. Poser le certificat en secret d'Actions fermerait le sujet ; la douzaine de scripts frères n'a pas encore suivi.

**À vérifier, non tranché :** le cron des Reçus déclare `maxDuration = 300`, alors que le plan Hobby plafonne les fonctions bien plus bas. Rien ne l'a encore prouvé en conditions réelles — au premier vrai passage, regarder si la fonction est coupée en cours de route.

**Régénérer un seed déjà appliqué doit produire les mêmes octets.** Le
générateur de l'Academy sert deux formations ; ajouter un champ au jsonb des
ressources — même à `null` — aurait changé l'empreinte de `0058`, appliquée
depuis le 22/08, et le runner aurait affiché « ⚠ modifiée depuis » sans jamais
la rejouer. Le champ n'est donc écrit que sur les ressources qui le portent, et
le `git diff` du fichier généré est le contrôle à faire après chaque
modification du générateur.

Quand on perd du temps deux fois sur le même problème, ajoute-le ici.
